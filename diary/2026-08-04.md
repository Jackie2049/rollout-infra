# 2026-08-04 Diary

## Summary

verl #6830 完整 Phase ④（patch + 3 单测 + pytest 39/39 全过），是本轮旗舰候选。MoonEP #17 静态验证发现报告者分析 gap。趋势项目推荐产出（MoonEP 首选）。

## 工作内容

### 08-04 上午规划 + 侦察

- 规划上午工作（5 block），用户扩展范围到 verl/ROLL/pytorch/MoonshotAI + 趋势项目推荐
- 后台 agent 侦察：verl 无认领 bug（避开 8 个已认领）、趋势项目（MoonEP/FlashKDA/AReno/Dressage/slime）
- 用户明确硬规则：**issue/PR 只能在 fork 上建，探索阶段绝不在主仓库建，对外提交需一起**。更新 memory `no-upstream-issue-pr-without-consent`

### verl #6830 完整 Phase ④（旗舰）

`process_validation_metrics` 崩在 sparse reward_extra_info 的 None：
- 根因：`metric_utils.py` 聚合循环 skip-filter 只跳 string 不跳 None → `np.mean([None])` TypeError
- 4090 服务器（verl-pr env, torch 2.6.0）复现 TypeError（n=1 组 '/' + maj 路径 '+'）
- patch：聚合循环开头过滤 None + pred 按位置对齐（`zip strict=True`）+ 删除旧外层 has_pred
- 补 3 单测（sparse None / 全 None 跳过 / sparse+pred 对齐）到 `test_metric_utils_on_cpu.py`
- **pytest 全 39 passed**（3 新 + 36 现有无回归），4.28s
- `help wanted` 标签 + 0 评论 0 assignee → 竞争风险极低
- 性价比 40，本轮最高。patch 在本地未碰远端，等用户审阅后一起在 fork 开 PR

### MoonEP #17 静态验证

`create_nvl_dist_multicast_tensor` 错误路径资源泄漏：
- mc_fd 泄漏确认（`_exchange_ipc_fds` 抛 → `os.close(mc_fd)` 跳过），trivial 修复（try/finally）
- mc_handle 泄漏确认，但**发现报告者分析 gap**：说"复用 `nvl_release_mem_handle`"，实际 multicast 对象无 Python release binding（bind 后靠 mc_view deleter，bind 前 fail 无释放路径）→ 需新 C++ binding
- MoonEP 是 NVSwitch SHARP 硬件相关，4090 测不了，验证止于静态分析
- 推荐：先 comment 互动指出 binding gap（MoonEP review 慢，7 PR 堆着，先建立信任）

### verl 贡献政策（CLAUDE.md）

读到 verl CLAUDE.md：禁纯 agent PR（需人类端到端辩护）、AI PR 需声明 AI 协助 + 测试结果、禁 busywork PR、提交前查重。正好契合用户"审阅后一起提交"要求。

## 文件产出

- `open-source/verl/candidate-validation-metrics-none.md`（#6830 完整 Phase ④）
- `open-source/verl/scan-0804-1016.md`（本轮扫描报告 + 跨仓库推荐）
- `open-source/moonshot/tracker.md`（MoonshotAI 贡献追踪）
- `open-source/moonshot/candidate-moonep-buffer-leak.md`（#17 + mc_handle gap 发现）
- `open-source/verl/tracker.md`（更新 #6830 为 verified-works）
- README.md（加 moonshot 区 + 工作日志）

## 关键约束记录

用户硬规则（存 memory）：issue/PR 只能在 fork（Jackie2049/<repo>）上建；探索阶段绝不在主仓库建 issue/PR；对外提交必须用户审阅后一起。

## 下一步

- #6830 等用户审阅 patch → 一起在 `Jackie2049/verl` fork 开 PR（pre-commit + 查重先做）
- #17 MoonEP 推荐先 comment 互动，等 maintainer 认可再提 PR
- verl 备选（#6786/#7146/#6690）待深挖
