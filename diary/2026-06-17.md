# 2026-06-17 学习日志

## Entry 227: MindIE Turbo + Inductor Triton Codegen + Training-Deployment Pipeline + Watermark+BudgetRefiner Synthesis

★★★★★★★★★ MindIE Turbo DeepSeek inference source-level analysis (from background agent):
  → 3 pillars: compose-level fusion + MLA native kernel + spec decode in latent space → 2-3x latency reduction
  → npu_dequant_swiglu_quant: 6→1 per expert → MOST portable lesson → Triton kernel for SM89 = vLLM contribution opportunity
  → fused_deep_moe: ENTIRE MoE → 1 kernel → 24x → impossible on CUDA (register pressure + parallelism mismatch)
  → 5 portable lessons: dequant+SwiGLU+quant Triton → MLA preprocess fusion bottleneck → Spec V2 overlap → TritonMLA only RTX 4090 option → BudgetRefiner universal
  → vLLM-Ascend = ONLY NPU framework preserving BudgetRefiner SLO → MindIE Turbo = scheduling black box

★★★★★★★★★ PyTorch Inductor Triton codegen pipeline (from background agent):
  → Complete pipeline: SchedulerNode → 3-layer fusion → SIMDScheduling → TritonKernel → CachingAutotuner → Triton JIT
  → ★★★★★★★★ CachingAutotuner: custom (parallel precompile + disk cache + coordinate descent) — NOT Triton built-in
  → ★★★★★★★★ persistent_reduction: RBLOCK=constexpr + XBLOCK=autotuned → batch-dependent accumulation → ROOT CAUSE CONFIRMED
  → ★★★★★★★★ Combo kernels + #187275: SAME root cause class → persistent reduction dimension handling weakness → CONFIRMS diagnosis!
  → Key code locations: choices.py 639-647 (insertion point) + 472-506 (reduction_split_factor precedent)

★★★★★★★★★ RTX 4090 training-to-deployment pipeline end-to-end guide:
  → 6 stages: LoRA GRPO → merge → INT4 quantize → serve → spec decode → continuous training hot-swap
  → Covers dense (Qwen3-1.7B), MoE (Qwen3-MoE AutoEP), OPD (Qwen2.5-0.5B student)
  → ★★★★★★★★ LoRA hot-swap = unique advantage → A/B testing → incremental updates without re-deploy
  → ★★★★★★★★ SGLang deterministic → kernel-level batch invariance → better for quality-sensitive (GRPO rollout)
  → ★★★★★★★★ vLLM enforce_eager → -10-20% throughput → recommended for throughput-focused serving

★★★★★★★★★ Watermark + BudgetRefiner complementary synthesis:
  → Watermark handles KV cache pressure (reactive, memory) → BudgetRefiner handles compute time pressure (proactive, compute)
  → ★★★★★★★★ Together: BudgetRefiner reduces token load → fewer KV blocks → Watermark triggers less → feedback loop → approaching zero preemptions!
  → 3 integration points: token_budget (dynamic) + decode-first (reorder) + max_seqs (dynamic limit)
  → RTX 4090 profile_table.csv = UNIQUE data → #1 contribution priority

★★★★★ GPU experiment queue restructured (P10→P1 priorities):
  → P10 UNIQUE: BudgetRefiner profile data → NO other contributor has RTX 4090 data
  → P9: batch invariance repro → Fusion Guard validation
  → P6 UNIQUE: AutoEP MoE smoke test → ONLY framework supporting MoE on RTX 4090

★★★★★ Bug fix: deepspeed_config_generator.py scenario_name parameter now passed correctly
★★★★★ GPU status: both OFFLINE
★★★★★ Project stats: 930+ commits, 236+ project notes, 355+ tools
