# 2026-06-17 学习日志

## Entry 227: MindIE Turbo + Inductor Triton Codegen + Training-Deployment Pipeline + Watermark+BudgetRefiner Synthesis

★★★★★★★★★ MindIE Turbo DeepSeek inference source-level analysis (from background agent):
  → 3 pillars: compose-level fusion + MLA native kernel + spec decode in latent space → 2-3x latency reduction
  → npu_dequant_swiglu_quant: 6→1 per expert → MOST portable lesson → Triton kernel for SM89 = vLLM contribution opportunity
  → fused_deep_moe: ENTIRE MoE → 1 kernel → 24x → impossible on CUDA (register pressure + parallelism mismatch)
  → 5 portable lessons: dequant+SwiGLU+quant Triton → MLA preprocess fusion bottleneck → Spec V2 overlap → TritonMLA only RTX 4090 option → BudgetRefiner universal
  → vLLM-Ascend = ONLY NPU framework preserving BudgetRefiner SLO → MindIE Turbo = scheduling black box

★★★★★★★★★ PyTorch Inductor Triton codegen pipeline (from background agent):
  → Complete pipeline: SchedulerNode → 3-layer fusion → SIMDScheduling → TritonKernel → CachingAutotuner → Triton JIT
  → ★★★★★★★★ CachingAutotuner: custom (parallel precompile + disk cache + coordinate descent) — NOT Triton built-in
  → ★★★★★★★★ persistent_reduction: RBLOCK=constexpr + XBLOCK=autotuned → batch-dependent accumulation → ROOT CAUSE CONFIRMED
  → ★★★★★★★★ Combo kernels + #187275: SAME root cause class → persistent reduction dimension handling weakness → CONFIRMS diagnosis!
  → Key code locations: choices.py 639-647 (insertion point) + 472-506 (reduction_split_factor precedent)

★★★★★★★★★ RTX 4090 training-to-deployment pipeline end-to-end guide:
  → 6 stages: LoRA GRPO → merge → INT4 quantize → serve → spec decode → continuous training hot-swap
  → Covers dense (Qwen3-1.7B), MoE (Qwen3-MoE AutoEP), OPD (Qwen2.5-0.5B student)
  → ★★★★★★★★ LoRA hot-swap = unique advantage → A/B testing → incremental updates without re-deploy
  → ★★★★★★★★ SGLang deterministic → kernel-level batch invariance → better for quality-sensitive (GRPO rollout)
  → ★★★★★★★★ vLLM enforce_eager → -10-20% throughput → recommended for throughput-focused serving

★★★★★★★★★ Watermark + BudgetRefiner complementary synthesis:
  → Watermark handles KV cache pressure (reactive, memory) → BudgetRefiner handles compute time pressure (proactive, compute)
  → ★★★★★★★★ Together: BudgetRefiner reduces token load → fewer KV blocks → Watermark triggers less → feedback loop → approaching zero preemptions!
  → 3 integration points: token_budget (dynamic) + decode-first (reorder) + max_seqs (dynamic limit)
  → RTX 4090 profile_table.csv = UNIQUE data → #1 contribution priority

★★★★★ GPU experiment queue restructured (P10→P1 priorities):
  → P10 UNIQUE: BudgetRefiner profile data → NO other contributor has RTX 4090 data
  → P9: batch invariance repro → Fusion Guard validation
  → P6 UNIQUE: AutoEP MoE smoke test → ONLY framework supporting MoE on RTX 4090

★★★★★ Bug fix: deepspeed_config_generator.py scenario_name parameter now passed correctly
★★★★★ GPU status: both OFFLINE
★★★★★ Project stats: 930+ commits, 236+ project notes, 355+ tools

## Entry 228: Megatron RouterReplay Source Reading + Coverage Gap Analysis

★★★★★★★★★ Megatron RouterReplay #4168 source-level analysis (207 lines):
  → 3 modes: RECORD (compute top-k + store) / REPLAY_FORWARD (skip top-k, use stored) / REPLAY_BACKWARD (pop from list)
  → ★★★★★★★★ REPLAY_FORWARD: scores.gather(1, top_indices) → deterministic routing → CUDA graph compatible!
  → ★★★★★★★★ Static buffers: [max_tokens, topk] per layer → combined [max_tokens, num_layers, topk] → CUDA graph pre-allocation
  → RTX 4090: EP=1 + RouterReplay + CUDA graph = deterministic MoE → BUT Megatron crashes (#5203) → need DeepSpeed equivalent!
  → ★★★★★★★★ Coverage gap identified: DeepSpeed AutoEP viable but NO RouterReplay → MoE GRPO CUDA graph stability needs this mechanism!

★★★★★★★★★ Megatron coverage gap analysis (from agent scan):
  → P1 gap: #6713 LoRA Adapter Export for Rollout → bridges training→inference → RTX 4090 deployment pipeline
  → P2 gap filled: RouterReplay #4168 → now covered with source reading
  → P3 gap: QB+AutoEP+LoRA end-to-end validation → needs GPU
  → P5 gap: Megatron-Bridge (NeMo2) LoRA → only existing LoRA path → need source reading
  → Well covered: #5349 QB routing, #5203 singleton crash, Mamba prefix caching, Lite #4885

★★★★★ Session stats: 935 commits, 237 project notes, 345+ tools
★★★★★ GPU status: both OFFLINE — all work CPU-only

## Entry 229: Background Agent Results + verl ContinuousToken + Watermark+BudgetRefiner Agent

★★★★★★★★★ All background agents completed and incorporated:
  → MindIE Turbo: 3 pillars + 5 portable lessons → already written as source reading
  → verl ContinuousToken: #6720/#6721 confirmed OPEN (not merged) → response_mask alignment preserves GRPO/PPO/CPPO → ZERO algorithm changes needed
  → Inductor Triton codegen: CachingAutotuner persistent_reduction → already written as source reading
  → Watermark+BudgetRefiner: confirmed complementary (zero conflicts) → already written as synthesis

★★★★★★★★★ verl ContinuousToken response_mask alignment detail:
  → mask=[1,0,1,0,...] = standard multi-step RL design → action tokens mask=1, observation tokens mask=0
  → ★★★★★★★★ ZERO changes needed to GRPO/PPO/CPPO algorithms → mask preserved after CT builder merges
  → CT solves decode-encode roundtrip problem → append-only → KV reuse → no token inconsistency

★★★★★ Updated verl-continuous-token-agent-loop-reading.md with response_mask alignment analysis
★★★★★ Project stats: 937 commits, 237 project notes, 346+ tools

## Entry 230: verl PR #6713 LoRA Export Source Reading + rLLM Step Merge Architecture

★★★★★★★★★ CRITICAL CORRECTION: PR #6713 is a **verl** PR, NOT a Megatron-LM PR!
  → Megatron-LM PR numbering is ~#5393, NOT #6713
  → Project notes previously labeled "Megatron #6713" → corrected to "verl #6713"
  → Updated cross-framework-lora-adapter-export-comparison.md with correct attribution

★★★★★★★★★ verl PR #6713 source-level analysis (from agent):
  → verl-project/verl PR #6713: "Megatron LoRA Adapter Export for Rollout" (DRAFT)
  → 4 files modified + 1 test: megatron_peft_utils.py, transformer_impl.py, bridge.py, model_forward_fused.py
  → Two new functions: gather_ep_lora_adapter_weights_for_vllm() + pack_3d_moe_lora_adapter_weights_for_vllm()
  → ★★★★★★★★ 3-repo coordinated: verl #6713 + verl-omni #169 + vllm-omni #4388 — all DRAFT, same author
  → EP=1 (RTX 4090): passthrough → zero gather overhead
  → ★★★★★★★★ Critical review issue: requires_grad_(True) on non-leaf tensor → runtime error → needs fix before merge
  → Megatron-Bridge = ONLY Megatron LoRA path → uses sharded format → verl bridges format conversion
  → ★★★★★★★★ RTX 4090: can't use verl Megatron backend for training (single GPU crash #5203) → useful for multi-GPU train → RTX 4090 serve bridge

★★★★★★★★★ rLLM Step Merge + SyncCoordinator architecture (from agent):
  → Client-server model: Tinker SDK pattern (NOT HTTP/IPC) → TrainingClient + SamplingClient → same process → zero-copy
  → SyncCoordinator: 172 lines → asyncio → staleness formulation → staleness_threshold=0 = on-policy
  → Step merge: prefix detection → delta extension → PR #576 (backend-agnostic MergedSegment + TokenOps Protocol)
  → ★★★★★★★★ bypass_mode: pi_old = rollout logprobs → no ref model → 18Ψ→3.8Ψ → RTX 4090 viable!
  → Tinker checkpoint NOT standard PEFT → export gap: needs tinker-cookbook conversion to HF format
  → ★★★★★★★★ RTX 4090: staleness_threshold=0, trigger_parameter_sync_step=4-8 → stream off-policy minimal staleness

★★★★★ Created: verl-pr6713-megatron-lora-export-reading.md (P1 gap filled)
★★★★★ Updated: cross-framework-lora-adapter-export-comparison.md (attribution corrected, verl #6713 details added)
★★★★★ GPU status: both OFFLINE — all work CPU-only
★★★★★ Project stats: 940+ commits, 238+ project notes, 347+ tools

## Entry 231: Megatron-Bridge LoRA Source Reading + SGLang Deterministic Inference Deep Dive

★★★★★★★★★ Megatron-Bridge (NeMo2) LoRA source-level analysis (P5 gap filled):
  → Core Megatron-LM has NO LoRA → ALL LoRA goes through Megatron-Bridge (standalone pip package)
  → 4 LoRA types: lora (fused), canonical_lora (unfused, PEFT-compatible), vlm_lora (VLM), dora (magnitude+direction)
  → create_peft() = central factory → config dict → PEFT adapter class → pre-wrap hook before DDP
  → 3 export paths: export_weights (full), export_adapter_weights (adapter-only), export_hf_weights (HF format)
  → Adapter-only export = incremental RL weight sync → PR #6713 enhances this path
  → TP=1 on RTX 4090: LoRALinear wraps nn.Linear → zero overhead → BUT bridge abstraction adds ~3-5%
  → MoE LoRA: routers excluded by default → EP=1 for single GPU → all experts in 24GB
  → ★★★★★★★★ RTX 4090 ranking: Megatron-Bridge LoRA #3-4 → overhead not optimized for single GPU
  → ★★★★★★★★ BUT unique value: MoE LoRA → DeepSpeed cannot do MoE easily → Bridge enables it!

★★★★★★★★★ SGLang deterministic inference deep dive (from agent):
  → 7 aten overrides with tl.constexpr BLOCK_SIZE → KERNEL-level guarantee → bypasses Inductor entirely
  → ★★★★★★★★ SGLang added 3 more overrides beyond original 4: rms_norm, mm.dtype, bmm
  → murmur_hash32 Triton kernel: position+seed+vocab hash → Gumbel-max float64 → deterministic sampling!
  → ★★★★★★★★ No RNG state synchronization needed → entirely GPU-side → gold standard for GRPO
  → RadixAttention: C++ JIT radix tree → token-level (not block-level) prefix sharing → 7 eviction strategies
  → ★★★★★★★★ Triton backend recommended SM89: fixed split_tile_size=256 → split count depends on seq length NOT batch size
  → DeepGEMM disabled on SM89 → ALL matmuls use _matmul_persistent_triton constexpr → GOOD for determinism
  → rl_on_policy_target auto-enables deterministic inference → verl/rLLM RL frameworks
  → ★★★★★★★★ SGLang vs vLLM: KERNEL-level (constexpr) vs COMPILE-level (fusion guard) → fundamentally different layers
  → LoRA serving: Triton SGMV + chunked sgmv + RadixKey extra_key namespace isolation → MoE LoRA supported
  → ★★★★★★★★ Spec decode: 6 algorithms (EAGLE, DFlash, NGRAM, STANDALONE, FROZEN_KV_MTP) → RadixKey bigram mode for EAGLE

★★★★★ Created: megatron-bridge-lora-source-reading.md (P5 gap filled)
★★★★★ GPU status: both OFFLINE — all work CPU-only
★★★★★ Project stats: 942+ commits, 240+ project notes, 348+ tools
