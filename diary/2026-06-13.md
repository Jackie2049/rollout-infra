# 2026-06-13 日记

## 06:00 — DeepSpeed ZeRO源码深度阅读

### 背景

从06-12 session续接。完成torch.distributed+NCCL+weight formats三篇深度阅读后，用户要求学习DeepSpeed ZeRO源码。GPU服务器仍然离线(SSH超时)。继续源码阅读。

### 核心

DeepSpeed ZeRO = 分区消除冗余 + 按需聚合 + Prefetch重叠通信计算

关键发现:
- **ZeRO-1/2**: stage_1_and_2.py(3038行) → IPGBucket ping-pong buffer → reduce_bucket_size=500M → reduce-scatter(ZeRO-2) vs all-reduce(ZeRO-1) → round_robin梯度负载均衡
- **ZeRO-3**: stage3.py(3579行) → DeepSpeedZeroOptimizer_Stage3 → Init metaclass注入! → 狙击Module.__init__ → 创建后立即分区 → 全局透明!
- **Init metaclass**: partition_parameters.py(2376行) → 最巧妙设计 → 替换torch.nn.Module.__init__ → partition_after包装 → 子类init完成后分区 → Tensor构造函数也替换!
- **ZeROOrderedDict**: 替换module._parameters → __getitem__拦截 → forward时自动all_gather → 透明参数聚合!
- **PartitionedParameterCoordinator**: trace+prefetch → RECORD→COMPLETE→INVALID → 通信计算重叠! → __ongoing_fetch_events≤2 → 内存控制!
- **AllGatherCoalescedHandle**: 批量all-gather → 单次通信 → N参数→1次→延迟省N倍!
- **ZeRO-Offload**: CPU/NVMe offload → DeepSpeedCPUAdam → cpuadam_cores_perc=0.8 → pin_memory加速DMA
- **ZeRO-Infinity**: NVMe三级存储 → max_in_cpu限制 → pipeline_read预读 → 2GB/s带宽瓶颈
- **RTX 4090**: ZeRO-2+CPU offload=单GPU最优(全参数); LoRA+grad checkpointing更优(微调)

笔记: `notebook/fundamentals/deepspeed-zero-source-reading.md`

## 09:00 — Transformer Engine FP8源码深度阅读

### 核心

TE FP8训练 = Recipe体系 + Quantizer执行 + StateManager协调 + FP8 GEMM

关键发现:
- **Recipe体系**: DelayedScaling(Ada/Hopper, amax_history+scale=(FP8_MAX/amax)/2^margin) → Float8CurrentScaling(即时amax) → MXFP8BlockScaling(Blackwell) → NVFP4BlockScaling(FP4!)
- **Float8Quantizer**: per-tensor delayed scaling → tex.quantize(CUDA kernel) → scale×cast→FP8
- **FP8GlobalStateManager**: 全局状态+global_amax_buffer → autocast_exit集中reduce → fused kernel!
- **TE.Linear**: LinearFwdArgs打包 → input_quantizer→weight_quantizer→general_gemm(FP8 cublasLt) → output
- **Userbuffers**: comm+GEMM overlap → pipeline/ring_exchange → RTX 4090不支持multicast
- **RTX 4090**: DelayedScaling唯一可用; B≥4时1.48-1.59x加速; 无MXFP8/NVFP4

笔记: `notebook/fundamentals/transformer-engine-fp8-source-reading.md`

## 10:30 — ZeRO算法深度分析 + 跨框架对比

### 背景

用户要求不只是看代码，而是对ZeRO算法本身进行deep dive，包括数学原理和跨框架实现对比。

### 核心

ZeRO = 冗余消除 + 状态分区 + 按需聚合 + 通信不变

关键发现:
- **ZeRO数学推导**: DDP=16Ψ+KΨ → ZeRO-1=4Ψ+(12+K)Ψ/N → ZeRO-2=2Ψ+(14+K)Ψ/N → ZeRO-3=(16+K)Ψ/N → 完美线性缩放!
- **通信量**: ZeRO-1=2Ψ(all-reduce) → ZeRO-2=Ψ(reduce-scatter) → ZeRO-3=2Ψ(all-gather+reduce-scatter) → 总量不变!
- **跨框架对比**: DeepSpeed(Init metaclass+ZeROOrderedDict+trace prefetch) vs FSDP1(FlatParameter+hook) vs FSDP2(per-parameter+DTensor+torch.compile兼容) vs Megatron(TP+PP+DP)
- **哲学差异**: ZeRO-3"参数出生就分区" → FSDP2"参数先完整再shard" → 不同侵入程度!
- **ZeRO+TP+PP**: (16+K)Ψ/(TP×PP×DP) → 3D完美缩放 → 但需NVLink!
- **RTX 4090**: 单GPU最优 → ZeRO只在CPU offload场景有用 → LoRA不需要ZeRO!

笔记: `notebook/fundamentals/zero-algorithm-deep-dive.md`

## 12:00 — RDMA + AI Cluster Networking架构深度分析

### 背景

继续ZeRO深度分析后的学习。GPU服务器仍然离线。从RDMA基础(rdma-networking.md已有975行参考)出发，深入分析Verbs内核机制、RoCE v2包封装、集群拓扑、拥塞控制算法。

### 核心

RDMA = 内核旁路+零拷贝 + 网络拓扑决定通信效率 + 拥塞控制保证无损

关键发现:
- **QP State Machine**: RESET→INIT→RTR→RTS→ERROR 5步 → INIT只recv → RTR可recv远端 → RTS可send → 连接建立!
- **Doorbell机制**: ibv_post_send→用户态写WQE→doorbell(MMIO)→NIC DMA执行 → 全程零syscall! → 这就是内核旁路的核心
- **CQE 64B格式**: status+opcode+byte_len+qp_num → NIC DMA写入 → 用户态ibv_poll_cq轮询 → 延迟≈0.5μs!
- **SRQ共享接收**: N个QP共享1个SRQ → M buffer vs N² buffer → 内存省+简化管理
- **RoCE v2封装**: IB传输头+UDP(IP port 4791)+IP+Ethernet → 可路由 → vs IB原生LRH(LID路由)
- **Lossless Ethernet三要素**: PFC(802.1Qbb优先级暂停)+ECN(RFC 3168拥塞标记)+DCQCN算法 → 必须全部配置!
- **PFC vs IB Credit**: PFC可能head-of-line blocking+deadlock → IB Credit原生无损 → 简单但贵!
- **Fat-Tree拓扑**: 2层(leaf+spine)≤64节点 → 3层(leaf+spine+core)>64节点 → 多路径负载均衡!
- **Rail-aligned**: GPU_i↔NIC_i → 8 rails → 每rail独立AllReduce → 8x带宽聚合 → Meta/Azure方案!
- **DCQCN算法**: Rate=Rate×(1-α/2) → α=ECN比例 → CNP反馈 → Fast/Active/Hyper三阶段恢复 → 类DCTCP但RDMA定制!
- **NCCL IB Plugin**: ibv_post_send→RDMA Write→GPUDirect→DMA-BUF → Socket Plugin(TCP fallback) → RTX 4090只能用Socket!
- **真实集群**: Meta 16K GPU 3层fat-tree+8 rails → Azure HBv3 8×A100+8 IB → AWS EFA SRD(自研可靠传输)
- **RTX 4090**: 无RDMA+无NVLink+无IB → 只PCIe+TCP → AllReduce 2.76GB/s → 单GPU最优!

笔记: `notebook/fundamentals/rdma-ai-networking-deep-dive.md`
