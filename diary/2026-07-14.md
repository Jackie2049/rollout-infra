# 2026-07-14 Diary (Session 6)

## Summary
Project partner GPU server (4×A100-SXM4-80GB) is ONLINE! Setting up env-rollout on partner server for PR testing. University and matpool still offline.

## Work Done

### 1. GPU Status Check
- University (219.233.198.62): OFFLINE (timeout)
- Matpool (hz-t3.matpool.com:28959): OFFLINE (connection refused)
- **Partner (10.26.6.88:31954): ONLINE** — 4× A100-SXM4-80GB, GPU0/1/2 ~81GB free, GPU3 ~45GB free

### 2. Partner Server Setup (in progress)
- Workdir `/jiangdingfeng/zy/Termius/rollout/` created
- rollout-infra repo cloned from jackie2049/rollout-infra
- env-rollout conda env created (Python 3.12.13)
- PyTorch 2.8.0+cu128 being installed (download in progress)
- pip corrupted state encountered (missing RECORD files for torch, typing_extensions)
- Fix: removed corrupted packages, reinstalling with `--ignore-installed`

### 3. TRL #6361 Fix Applied and Verified on A100 Server
- Bug confirmed: `qwen3_5_think_training.jinja` L103 has `{%- if 'hés' in content and 'h³' in content %}` requiring BOTH tags
- Think markers in template: `hés` / `h³` (confirmed by hex dump of L103 bytes)
- When generation prompt emits `hés` but training content only has `h³`, the condition fails → empty reasoning_content → malformed output
- Fix applied: Added `{%- elif 'h³' in content %}` branch at L106 in both templates
  - qwen3_5_think_training.jinja: 8381 → 8685 chars, .orig backup saved
  - qwen3_5_nothink_training.jinja: 8384 → 8688 chars, .orig backup saved
- Verification: All 3 scenarios pass
  - Scenario 1 (bug case): think-end only → elif branch correctly parses reasoning_content and content
  - Scenario 2 (normal): both tags → if branch correctly parses
  - Scenario 3 (no tags): regular content → reasoning_content stays empty
- Fix script: `tools/apply_6361_fix.py`, test script: `tools/test_6361_fix.py`
- Shell encoding issues resolved by SCP + remote execution approach (instead of inline heredoc)

### 4. P8-1 Fix Applied and Verified
- Added warning at L709-714: `num_generations=1 degenerates GRPO to REINFORCE with zero advantage`
- Backup saved at grpo_trainer.py.p81.orig
- Warning-only change, no behavior modification, fully backward compatible

### 5. P7-2 Fix Applied and Verified
- Config: `top_n_sigma: float = field(default=0.0)` added after scale_rewards
- Trainer init: `self.top_n_sigma = args.top_n_sigma` added after scale_rewards
- Trainer clipping: 12-line block added after `nan_to_num(advantages)` at L2593
  - Clamps advantages to [mean - n*std, mean + n*std] when top_n_sigma > 0
  - Logs clip_upper, clip_lower, clipped_fraction metrics
- Default=0.0 → disabled, fully backward compatible

### 6. P9-1 Fix Applied and Verified
- Config: `bypass_mode: bool = field(default=False)` added after top_n_sigma
- Trainer init: `self.bypass_mode = args.bypass_mode` at L751
- Trainer bypass: Changed heuristic at L2425 from implicit to explicit
  - Old: `if self.args.gradient_accumulation_steps % generate_every != 0 or (...)`
  - New: `if not self.bypass_mode and (self.args.gradient_accumulation_steps % generate_every != 0 or (...))`
- Default=False → identical to current behavior, fully backward compatible

### 7. Comprehensive Verification
- All 4 patches verified structurally and via import check
- GRPOConfig.top_n_sigma = 0.0, GRPOConfig.bypass_mode = False
- GRPOTrainer imports OK with all new fields
- Backward compatibility confirmed: defaults preserve original behavior

### 4. Loop Task
- Scheduled recurring job 78f1b550 every 10 minutes
- Auto-expires after 7 days

## Status
- env-rollout: DONE (Python 3.12, torch 2.8.0+cu128, TRL 1.8.0)
- #6361 fix: DONE (applied and verified on both templates)
- P8-1 warning: DONE (applied and verified)
- P7-2 top_n_sigma: DONE (applied and verified, default=0.0)
- P9-1 bypass_mode: DONE (applied and verified, default=False)
- #6274 VLM grid slicing: DONE (applied and verified, needs runtime VLM test)
- rLLM #605: PLANNING (terminal-rl branch cloned; grouping already correct on terminal-rl; need to add grouping_key to TransformConfig + register grouped_grpo estimator)
