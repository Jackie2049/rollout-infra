# 2026-08-03 Diary

## Summary

建立开源贡献挖掘 SOP 流水线，整合进 `rollout-infra` 仓库。verl 首轮扫描产出 3 个候选，#7213 (del_local_ckpt_after_load bug) 为首选 P0。

## 工作内容

### 14:00 — verl-pr 基础设施搭建

- 清理 4090 服务器磁盘：删 `/home/zxw/verldir` (35G)、`verl080_prefix-sharing` conda env (9.8G)、rollout-prefix 模型检查点 (69G) → 释放 113G，从 83G 可用 → 196G
- 克隆 conda env `verl-pr`（基于 `verl080_fsdp`）
- 创建 `/home/zxw/verl-pr/{verl_main,candidates,repro,outputs}` 目录结构
- 上传 verl main 源码（`8bda422`）到服务器

### 15:00 — SOP skill 沉淀

- 写 `~/.claude/skills/jackie-verl-pr/SKILL.md`：5 阶段流水线 + P0-P4 优先级 + I×A×F/C 评分公式
- 优先级口径按用户要求：P0=bugfix, P1=健壮性, P2=显存, P3=可量化 perf（附"高失败率"警告）, P4=其他

### 17:30 — 决定整合进 rollout-infra

- 用户最初想建独立 `code-miner` 仓库，已建（私有空仓）
- 用户改主意：挖掘应作为 `rollout-infra`（AI Infra Learning Hub）的一部分
- 删除 `code-miner` 仓库（需 `delete_repo` scope，用户待处理）
- 在 rollout-infra 新建 `open-source/` 顶层目录，浅结构（项目直接作子目录，方便手机 GitHub app 浏览）

### 18:14 — verl 首轮 SOP 扫描

扫描 `volcengine/verl@8bda422`，产出 3 个候选：

1. **#7213 del_local_ckpt_after_load 不删 checkpoint** — P0，性价比 30。Issue 干净（无 assignee/评论/标签），根因在 `fsdp_checkpoint_manager.py:222` / `megatron_checkpoint_manager.py:763,977` 删除逻辑失效。**首选**。
2. **veomni transformer 类型标注 `int` 应为 `bool`** — P1，性价比 12。单行修，作为 #1 的附属。
3. **tool_parser.py 17 处静默吞异常** — P1，性价比 12。experimental/ 变动快，备选。

关键经验：
- verl 团队响应极快（#7200 蒸馏损失 bug 当天报告当天修 #7225）→ 扫到的干净 issue 要尽快动手
- verl 不使用 `good first issue` 标签（0 个），issue 信号靠 reaction 排序
- `delta_checkpoint_engine.py`（1210 行/80 commit）和 `vllm_fp8_utils.py`（1097 行）是开发热点，改动会被冲掉，避开

## 文件产出

- `open-source/README.md` — 挖掘区入口 + 目录结构说明
- `open-source/SOP.md` — 通用方法论（5 阶段 + 评分公式）
- `open-source/verl/tracker.md` — verl 贡献追踪（3 个活跃候选）
- `open-source/verl/scan-0803-1814.md` — 首轮扫描完整报告

## 下一步

- 等用户决定是否立即开 #7213 的 Phase ④ 验证（上 4090 复现 + 定位根因）
- 处理 `code-miner` 仓库删除（`gh auth refresh -h github.com -s delete_repo`）
