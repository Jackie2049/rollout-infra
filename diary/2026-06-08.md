# 2026-06-08 日记

## 10:00 — Triton Decode Attention Kernel 正确性修复验证

### Bug修复与验证

修复了之前Triton decode attention kernel的关键bug:

**根因**: padding positions (kv_positions >= S) 通过mask `other=0.0` 加载为K=0, 产生score=0。在online softmax中, `exp(0 - m_new) ≠ 0` → padding位置贡献非零attention weights → 腐化归一化 → 输出小5-16x。

**修复**:
1. `scores = tl.where(valid_kv, scores, float("-inf"))` → padding score=-inf → exp(-inf)=0
2. `p = tl.where(valid_kv, p, 0.0)` → 零化padding位置的attention weights

**验证结果**:
- Triton vs Naive: cos_sim=**1.000000**, max_diff=0.000732 → 完全正确!
- Triton vs SDPA(is_causal=False): cos_sim=**1.0** → 与正确baseline完全一致
- Triton vs SDPA(is_causal=True): cos_sim≈0.02-0.09 → 但这是SDPA的问题(而非Triton)

### SDPA is_causal=True 对Decode Q=1的Bug

发现一个重要认知: **SDPA `is_causal=True` 对decode (Q=1) 产生错误结果!**

- SDPA将Q视为position 0 → causal mask限制Q只能看K[0] → 只attention到1个position
- 但decode场景中Q应该是在position S → 可以看到所有K positions[0..S-1]
- 这解释了为什么FA2 decode在RTX 4090上3-34x更慢(加上layout转换开销) + 结果错误
- 正确做法: decode使用 `is_causal=False` 或专用decode kernel

### Full Benchmark RTX 4090

Triton decode kernel速度(7M, n_heads=8, d_head=32):
- Triton vs SDPA: 2.1-2.4x慢 (B≤32, S≤1024)
- Triton vs SDPA: 2.9-3.1x慢 (S=2048)
- Triton vs SDPA: 7.4x慢 (B=64, S=2048) — memory-bound瓶颈

GQA (B=8):
- MHA: Triton 2.2-2.8x慢 vs SDPA(expanded)
- GQA-4: Triton 2.5-2.8x慢, 但75% KV内存省(no expand!)
- GQA-2: Triton 2.2-2.8x慢, 但87.5% KV内存省

BLOCK_KV tuning: 16/32/64/128/256全≈0.062ms → 无影响

KV Memory Traffic: 99.2-100% 为KV读取 → decode完全memory-bound

### 核心洞察

- Triton decode kernel **正确性**已完全验证(cos_sim=1.0)
- Triton decode kernel **速度**不如SDPA → 生产用FlashInfer
- Triton kernel **教育价值**: 完整实现online softmax + GQA native + padding mask
- **SDPA `is_causal=True` 对Q=1是错误** → vLLM/SGLang使用FlashInfer专门处理decode

### 文件更新

- `tools/triton_decode_attn_benchmark_4090.py` — padding mask fix
- `tools/triton_decode_attn_debug.py` — debug验证脚本
- `results/triton_decode_attn_benchmark.json` — 完整benchmark结果
- MEMORY.md + benchmark-results.md — 更新结果