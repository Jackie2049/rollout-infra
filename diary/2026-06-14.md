# 2026-06-14 日记

## 09:00 — AI Safety Guardrails for Production Deployment深度分析

### 背景

GPU服务器仍然离线(SSH超时，已连续2天)。从06-13 session延续，fill知识gap最大领域AI Safety(之前1/5)。结合我们推理serving强项与安全部署实际需求。

### 核心

AI Safety Guardrails = Defense-in-Depth 5层 + 无单点完美 + 必须层叠

关键发现:
- **Defense-in-Depth 5层**: L1 Input(regex 1ms 60%)→L2 System(指令层级)→L3 Model(RLHF/CAI)→L4 Output(Llama Guard 3 95%)→L5 Runtime(熔断) → 每层拦截不同攻击!
- **NeMo Guardrails**: Colang 2.0声明式+Runtime引擎+6类Rails(Input/Output/Dialog/Retrieval/Execution/LLM) → 中间层架构!
- **Llama Guard 3**: Llama 3.1 8B→fine-tune→安全分类器 → 6类14子类+多语言+代码安全+多轮+解释输出 → GPU推理200-500ms!
- **Prompt Injection OWASP #1**: Direct+Indirect+Jailbreak → Canary Token+指令层级+多模型+清洗+过滤 → 无完美方案→层叠!
- **OWASP LLM Top 10**: 注入#1→不安全输出#2→数据投毒#3→DoS#4→供应链#5 → 10类全覆盖!
- **Red-Teaming**: Garak自动200+探针+MITRE ATLAS+人工6类测试 → pre+post deployment!
- **Content Moderation三级**: regex(1ms 60%)→分类器(50ms 85%)→Llama Guard 3(200ms 95%) → 分级部署!
- **RTX 4090**: inline只regex(+1ms 6% ITL) → 其他async → 安全+性能兼顾! vs A100/H100可inline分类器!
- **Production检查清单**: pre-deployment(7项)+deployment(8项)+post-deployment(6项) → 全链路!

笔记: `notebook/fundamentals/ai-safety-guardrails-production-deep-dive.md`

## 11:00 — Data Pipeline & Curation for LLM Training深度分析

### 背景

GPU服务器仍然离线。AI Safety guardrails完成后，继续fill知识gap第二大数据领域Data Pipeline(之前2/5)。数据质量>模型架构是训练最关键因素，填补此gap对AI专家定位至关重要。

### 核心

Data Pipeline = 数据质量>模型架构 → 质量×3>数量 → curated 5TB≈raw 30TB

关键发现:
- **质量×3>数量**: FineWeb 250B→1.3TB → 8B+FineWeb=70.5% MMLU vs 250B原始=40-50% → 差距20+百分点!
- **DCLM证明**: 同架构不同数据 → 46%→64.8% → 18.8% → 数据决定性! 不改模型只改数据!
- **MinHash LSH去重**: Jaccard阈值=0.7 → k=128 hash → b=16 bands r=8 rows → 去除30-40%近似重复 → 最关键步骤!
- **Quality Filtering**: heuristic(5-10%)+learned classifier(15-25%) → 过滤95% → 5TB高质量 → 多样性保留!
- **DoReMi**: minimax excess loss → L_excess=L_domain-L_ref → 最优domain比例 → 5-10%性能提升 → 不改架构!
- **Data Contamination**: 13-gram overlap检测 → 污染vs投毒(无意vs恶意OWASP#3) → 去污染后真实性能5-10%虚高修正
- **Synthetic Data**: Self-Instruct(Alpaca 52K) + RL自我博弈(R1-Zero/AlphaProof) → 但不能纯合成→model collapse!
- **Data Wall**: 2024-2026人类高质量文本耗尽 → RL自我博弈(有验证器领域)是突破路径 → 无限数据!
- **生产Pipeline**: FineWeb(最严格1.3TB)+DCLM(证明性)+Nemotron-CC(商业30TB)+Datatrove(模块化开源)
- **RTX 4090**: LoRA微调+高质量子集+Self-Instruct+GRPO → 小模型高效路径 → MinHash+quality CPU可行!

笔记: `notebook/fundamentals/data-pipeline-curation-deep-dive.md`

## 12:00 — Multimodal AI (Vision-Language) Deep Dive

### 背景

GPU服务器仍然离线(已连续3天)。Data Pipeline完成后，继续fill最大知识gap Multimodal AI(VLM之前1/5)。从CLIP到Gemini的4代架构演进，结合RTX 4090推理优化经验。

### 核心

VLM=4代架构演进 → CLIP→LLaVA→InternVL→Gemini → 趋势=原生多模态

关键发现:
- **4代演进**: CLIP(2021)双编码器对比→LLaVA(2023)MLP投影+指令微调→InternVL(2024)InternViT-6B+动态分辨率→Gemini(2025)原生多模态
- **CLIP**: InfoNCE损失+ViT+Text Transformer→zero-shot 76.2% → 但不能生成 → 需LLM组合!
- **LLaVA**: MLP projection(~8M)+Vicuna-7B → 简单投影够用! → Q-Former被认为over-engineered!
- **InternVL**: InternViT-6B(比CLIP 14x)+动态分辨率448px → OCR/图表突破! MMMU=84.3%!
- **动态分辨率**: 图片→按aspect→N tiles→448×448→拼接 → thumbnail+局部 → 全图理解!
- **Projection**: MLP主流(~8M保留全部) vs Q-Former(~188M压缩到32) → ViT够强时MLP就够了!
- **Vision Encoder**: patch=14标准 → 多分辨率训练(SigLIP-2) → 位置嵌入(learned+bicubic)
- **三阶段训练**: pretrain(projection only)→SFT(LLM LoRA)→DPO(全模型) → 标准!
- **RTX 4090**: 7B INT4+INT8 ViT→7-8GB→24GB可行 → ViT 15-30ms → FlashAttention-2必须!
- **Phi-3.5-Vision-4B**: INT4→5GB → 最舒适 → 大量KV空间 → RTX 4090最优VLM!
- **2026趋势**: 原生多模态(Gemini方向)+Agent VLM+Token压缩+Video理解+小型高效VLM

笔记: `notebook/fundamentals/multimodal-vlm-deep-dive.md`

## 13:00 — Continual Learning深度扩展

### 背景

GPU服务器仍然离线(已连续3天)。Multimodal VLM完成后，继续fill知识gap #1 Continual Learning(之前1/5)。结合RL训练经验(SFT→GRPO=零泛化gap)与LoRA expertise。

### 核心

Continual Learning = Stability-Plasticity Dilemma → 2025工业标准 = Data Replay 5-15% + LoRA Routing + Model Merging

关键发现:
- **Data Replay 5-15%**: OLMo证明 → 5%混合→遗忘从60%→15% → 4x stability → 5% overhead → 巨大ROI! → 2025最practical!
- **Self-Distillation(L2KD)**: 旧模型作teacher → KL(new||old) → 不需存储旧数据 → 与LoRA组合更快!
- **LoRA CL**: O-LoRA(正交=5-10%遗忘)→InfLoRA(influence=3-8%)→CL-LoRA(routing)→Hierarchical(shared+专用)
- **PackNet+LoRA**: 双层隔离 → PackNet freeze底层 → LoRA adapt顶层 → 更强stability!
- **Model Merging**: Task Arithmetic α=0.75 → 零遗忘 → 但需task边界 → 我们已实测100%!
- **2025工业实践**: Data Replay+LoRA Routing+Model Merging = 三大标准 → consensus!
- **verl GRPO**: KL penalty = 隐式Data Replay+EWC → 天然stability-plasticity balance → 无需额外方法!

笔记: `notebook/fundamentals/continual-learning-deep-dive.md`

## 14:00 — Agent System Architecture Deep Dive

### 背景

GPU服务器仍然离线(已连续3天)。Continual Learning完成后，继续fill知识gap Agent Systems(之前2/5)。Claude Code本身就是Agent系统(Level 1 LLM+tools) → 直接体验!

### 核心

Agent=Planning+Tool Use+Memory+Execution → 推理仅40%延迟 → 其他60%

关键发现:
- **ReAct**: Reasoning+Acting交织 → think→act→observe → Foundation Pattern → 2022基础!
- **Toolformer**: 自监督学习 → 何时调用+调用哪个+传什么+如何整合 → LLM内建工具能力!
- **Function Calling**: 2025并行+strict+JSON mode → 结构化 → 生产可靠!
- **MCP Protocol**: Anthropic标准化 → 任何LLM→任何工具 → 统一 → Claude Code=MCP client!
- **Agent延迟**: 推理40%+API30%+Prompt重建20%+Parse10% → GPU不是瓶颈 → 系统开销是!
- **SGLang KV reuse**: RadixAttention → 跨步骤共享KV → 20-40%减 → 5.5s→3.2s!
- **Multi-Agent**: AutoGen(对话)+LangGraph(图)+CrewAI(角色)+Swarm(handoff)+MetaGPT(SOP) → 5框架!
- **Anthropic建议**: "从最简开始" → Level 1(LLM+tools)够用 → Claude Code证明!
- **RTX 4090**: 7B INT4+SGLang → 3-step agent ~2s → 实时 → Cascade Serving(Phi-3 routing+7B reasoning)!
- **Cascade Serving**: 小模型routing+大模型reasoning → 分级 → 24GB单GPU → a16z报告2025趋势!

笔记: `notebook/fundamentals/agent-system-deep-dive.md`
