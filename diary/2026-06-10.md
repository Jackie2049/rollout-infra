# 2026-06-10 日记

## 10:00 — RAG Systems Deep Dive + Simulator

### 背景

GPU服务器仍然不可达(SSH超时)。继续在本地推进理论学习+模拟器工具开发。

### RAG Systems Deep Dive (新笔记)

创建了 `notebook/fundamentals/rag-systems-deep-dive.md`，覆盖9个主题:

1. **RAG范式演进**: RAG-1.0(固定chunk)→RAG-2.0(hybrid search)→RAG-3.0(Self-RAG/CRAG/GraphRAG)
2. **Embedding Models**: BGE/D=1024, Matryoshka(前缀截断→D=256→98%recall→75%存储省), 量化FP16→99.5%/INT8→97-99%/Binary→90-95%+rerank→97%
3. **Chunking Strategies**: 5种(Fixed/Recursive/Semantic/Document/Agentic), Anthropic Contextual Chunking(加prefix→+5%recall), Goldilocks Zone=256-512
4. **Vector Database**: FAISS GPU→250x faster, HNSW=生产首选(M=16/ef=100→recall 95%), IVF+PQ=存储10x省
5. **Hybrid Search**: Dense+BM25+RRF(k=60)→互补→+5-10% recall, Reranking(cross-encoder→+5% recall), ColBERT(token-level→+7%但存储100x)
6. **Self-RAG/CRAG/GraphRAG**: Self-RAG=[Retrieve]/[IsREL]/[IsSUP] token→自适应, CRAG→web search fallback, GraphRAG→KG→全局推理
7. **RAG Serving Architecture**: 单GPU全栈(4.83GB→可行!), 分层(生产最优), Semantic Cache(30-50%命中→吞吐↑30-50%), Prefix Sharing(RAG天然适合!)
8. **RAGAS Evaluation**: Faithfulness > Recall > Precision(防幻觉最重要!), 高faithfulness低recall→RAGAS 0.833 > 高recall低faithfulness→0.741
9. **RTX 4090 RAG**: 7B INT4+BGE FP16+FAISS IVF+PQ→4.83GB→24GB可行, pipeline 47.5ms(cache miss)/27.5ms(fast)/6ms(cache hit)

### RAG Simulator (新工具)

创建了 `tools/rag_system_simulator.py`，5个模拟器类:

- **EmbeddingModel**: 量化impact(FP16=99.5%/INT8=100%/Binary=79.8% quality), Matryoshka截断, Binary+FP16 rerank hybrid
- **ChunkingSimulator**: 4策略(Fixed/Recursive/Semantic/Contextual)×4 chunk_size, Goldilocks Zone验证
- **HybridSearchSimulator**: Dense/BM25/RRF/Linear fusion+Cross-encoder reranking→7.4x improvement
- **RAGEvaluationSimulator**: RAGAS 4维度+Faithfulness-First验证
- **RTX4090RAGServingModel**: Pipeline latency+throughput+memory budget分析

关键结果:
- Contextual chunking=0.934最高recall(Anthropic方法+5%!)
- Faithfulness-First: 98% faithfulness → RAGAS 0.833 > 95% recall but 60% faithfulness → 0.741
- RTX 4090: 1M docs INT8→6.7GB→fits 24GB, throughput 21-37 QPS(with cache)
- RAG overhead=27.5ms(+138%)→但recall↑20%+幻觉↓50%

### RAG Core Laws

1. Retrieval-Quality Law: answer质量 ∝ 检索质量
2. Faithfulness-First Law: 防幻觉 > 高recall
3. Chunk-Goldilocks Law: chunk_size=256-512→recall peaks
4. Hybrid-Supplement Law: Dense+Sparse互补→+5-10%
5. Cache-Amplification Law: semantic cache→30-50% throughput↑
6. Separation-Law: 分层独立优化→最优

## 14:00 — Diffusion Models Deep Dive + Simulator

### Diffusion Models Deep Dive (新笔记)

创建了 `notebook/fundamentals/diffusion-models-deep-dive.md`，覆盖9个主题:

1. **扩散范式演进**: DDPM→DDIM→Flow Matching→Rectified Flow→Consistency Models 4代
2. **DDPM**: score function→噪声预测ε→推理1000步→太慢但质量好
3. **DDIM**: 确定性ODE→跳步加速→50步→同一训练不同采样
4. **Flow Matching**: 向量场v而非score→OT-CFM→最短路径→SD3用!
5. **Rectified Flow**: Reflow→路径变直→1-4步→SD3/Flux核心→2025生产首选!
6. **Consistency Models**: 自一致性→1步→CD蒸馏→但质量天花板低
7. **Architecture**: U-Net→DiT→MMDiT→Video DiT → Transformer统一→scaling law类似LLM
8. **Latent Diffusion**: VAE 48x压缩→latent空间diffusion→VAE decode瓶颈!
9. **RTX 4090**: DiT compute-bound(≠LLM decode) → VAE memory-bound → 大图瓶颈!

### Diffusion Simulator

`tools/diffusion_model_simulator.py` 4个模拟器:
- SD3-like 2B 4-step RF: **0.51s on RTX 4090** → 实时!
- VAE decode瓶颈: 768²=60% / 1024²=88% → VAE dominates!
- Trajectory straightness: DDPM curvature=0.5→100步 / RF=0.005→4步 → 直度决定速度!
- DiT-8B→OOM(49.7GB) / 2B→fit / INT8 8B→16GB→可行

### Diffusion Core Laws

1. Step-Quality Law: RF 4步≈DDIM 50步 → 12.5x加速
2. Latent-Space Law: 48x压缩 → 必需品
3. Trajectory-Straightness Law: 直度决定步数 → RF→4步
4. Architecture-Scaling Law: DiT类似LLM → 可预测
5. Compute-Bound Law: DiT=compute-bound → ≠LLM decode
6. VAE-Bottleneck Law: VAE=memory-bound → 大图瓶颈

## 20:00 — vLLM PR #45148 (NIXL KV connector metrics documentation)

### 背景

修复之前PR #44439的push失败问题(旧fork落后upstream→rebase冲突→数百merge conflicts)。

### 解决方案

- `git rebase --abort` 清理broken rebase状态
- 从最新upstream main创建fresh branch `doc/nixl-kv-connector-metrics-v2`
- 手动重新apply 2处文档改动(metrics.py docstrings + disagg_prefill.md metrics section)
- 用auth token via gh-proxy.com成功push到Jackie2049/vllm fork
- 删除旧branch `doc/nixl-kv-connector-metrics`
- 创建新PR #45148: https://github.com/vllm-project/vllm/pull/45148

### 文档改动内容

1. **metrics.py**: KVConnectorLogging class docstring(observe→aggregate→reduce→log pipeline), observe()和log()方法docstrings(TP>1时stats pre-aggregated semantics)
2. **disagg_prefill.md**: "Interpreting KV Transfer Metrics" section, 含3列表格(metric/semantics/TP>1 note) + tip(per-rank throughput×TP degree=system throughput)

### GPU状态

高校服务器(219.223.198.62)仍然不可达(SSH超时)。

## 22:00 — Distributed Systems Deep Dive Extended + Simulator

### 背景

扩展已有的distributed-systems-deep-dive.md, 添加6个新章节(7-12)和simulator工具。GPU仍不可用。

### 新增章节

- **§7 Failure Detection**: φ-accrual vs timeout → φ-accrual 0% FP rate vs timeout 0.19-55.7% → 自适应检测远优!
- **§8 Clock & Ordering**: Lamport(partial)→Vector(complete causal)→DynamoDB用vector clocks→AI Infra用step number(≈Lamport)
- **§9 Replication & Leader Election**: Primary-Backup/Chain/Quorum + Raft/Bully/Ring算法 + split brain prevention
- **§10 Distributed Storage**: etcd(CP/Raft→Kubernetes/Ray GCS) vs Redis(AP/sharded→feature store/model cache)
- **§11 PACELC Theorem**: 扩展CAP → P(A/C) + E(L/C) → Training=PC/EC → Inference=PA/EL → Checkpoint=PC/EC
- **§12 Core Laws 7-10**: Detection/Ordering/Replication-Choice/PACELC定律

### Distributed Systems Simulator (新工具)

`tools/distributed_systems_simulator.py` 5个模拟器类:
- **ConsensusSimulator**: Raft/Paxos/BFT对比 → 5节点0-3故障 → Raft可选举/Paxos达成共识/BFT需3f+1
- **FailureDetectorSimulator**: Timeout vs φ-accrual → φ-accrual **0% FP** vs timeout 0.19-55.7% → 生产推荐φ-accrual!
- **ClockOrderingSimulator**: Lamport vs Vector → Lamport不能检测并发/Vector可以→AI Infra用step number够了
- **ReplicationSimulator**: Primary-Backup/Chain/Quorum → 训练=PB(coordinator)/推理=W=1/R=1/PD=Chain
- **PACELCSimulator**: Training=PC/EC/Inference=PA/EL/PD=PA/EL/Checkpoint=PC/EC

关键洞察: **AI Infra不是单一PACELC选择 → 训练=一致性 / 推理=可用性 / 不同组件不同需求!**

### vLLM PR更新

- PR #45148 DCO修复: 添加Signed-off-by行 → force push → DCO应通过
- PR状态: OPEN, mergeable, ReadTheDocs preview生成中

## 23:30 — AI Serving Observability Deep Dive + Simulator

### 背景

可观测性是AI Infra从开发到生产的关键桥梁。GPU仍不可用。

### AI Serving Observability Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-serving-observability-deep-dive.md`，7个主题:

1. **Metrics系统**: vLLM V1 Prometheus (gpu_cache_usage, num_requests_running, TTFT/ITL histogram, prefix_cache_hit_rate, KV connector stats)
2. **结构化日志**: vLLM当前非JSON → 生产应JSON → request_id关联
3. **Distributed Tracing**: OpenTelemetry → vLLM尚未集成 → 未来关键改进
4. **SLO/SLA管理**: 4核心SLI (TTFT<500ms P99, ITL<100ms P99, throughput>1K tok/s, availability 99.9%)
5. **Autoscaling**: HPA vs sleep/wake → sleep/wake 30x更快 → RTX 4090推荐sleep/wake
6. **Production Debugging**: 5常见问题 (OOM/高延迟/GPU故障/低吞吐/请求drop) + 诊断流程
7. **Core Laws**: Observability/SLO-Driven/Cache-Utilization/Cold-Start/Debugging-Hierarchy/Error-Budget/Prefix-Visibility

### AI Serving Observability Simulator (新工具)

`tools/ai_serving_observability_simulator.py` 4个模拟器:
- **MetricsSimulator**: vLLM metrics → TTFT P99=220ms, ITL P99=10ms, KV=95%
- **SLOSimulator**: SLO合规检查 → TTFT/ITL通过但availability 99%未达99.9%
- **AutoscalingSimulator**: HPA vs sleep/wake → sleep/wake 30x快 → Error budget第10天耗尽
- **IncidentResponseSimulator**: 4种incident+runbook → OOM MTTR≈14min/GPU故障≈130min

关键洞察: **可观测性=生产必要条件 → 无监控=盲飞 → SLO驱动运维 → sleep/wake比HPA更适合AI serving**

### vLLM Issue #33267调研

发现已有PR #34062/#36244(均closed) → 可能LayerName hoisting(torch>=2.11)已解决核心问题 → 留了澄清问题

## 01:00 — AI Data Flow Architecture Deep Dive + Simulator

### 背景

GPU服务器仍然不可达。继续本地学习+工具开发。

### AI Data Flow Architecture Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-data-flow-architecture-deep-dive.md`，7个主题:

1. **Inference数据流**: vLLM V1 7步路径(HTTP→Tokenizer→Scheduler→ModelRunner→GPU→Sampling→Response)
2. **Training数据流**: verl RL 14步(Dataset→Forward→Reward→Backward→Optimizer→Checkpoint)
3. **Serialization格式**: SafeTensors(快+安全) vs pickle(慢+不安全) → 生产必须SafeTensors!
4. **Checkpoint管理**: 同步barrier(一致但慢) vs 异步+版本管理(verl=最优)
5. **Model Registry**: HF Hub+safetensors → vLLM HF download→load→serve → 简单!
6. **Feature Store**: online(Redis/AP) vs offline(HDFS/CP) → Feast统一 → 双写一致性挑战
7. **7 Core Laws**: Incremental-Transfer/Serialization-Cost/Compression-Ratio/Checkpoint-Consistency/Transfer-Bottleneck/Feature-Store-Dual-Write/Data-Flow-Debugging

### AI Data Flow Simulator (新工具)

`tools/ai_data_flow_simulator.py` 6个模拟器类:

- **InferenceDataFlowSimulator**: 7步推理路径+增量传输(>90%通信量节省)+TTFT/ITL估算
- **SerializationSimulator**: 5格式对比(PyTorch/SafeTensors/GGUF/FP8/BF16) → SafeTensors最优
- **CheckpointSimulator**: 3策略(sync/async/async+versioning) → verl异步+版本管理=最优
- **TransferBottleneckSimulator**: 6场景(PCIe/NVLink/Ethernet/HBM) → INT4缓解PCIe瓶颈
- **FeatureStoreSimulator**: online vs offline → 500x速度差距 → 双写一致性lag=5s
- **DataFlowDebuggingSimulator**: 4层debugging+5场景 → 平均27min解决 → metrics最快

关键洞察: **增量传输>90%通信节省 → safetensors是生产必需 → INT4量化=75%省但需fused kernel → verl异步checkpoint=最优**

### vLLM PR更新

- 旧PR #45148被意外关闭 → 创建新PR #45157 → OPEN, MERGEABLE

## 03:00 — AI Serving Cost Optimization Deep Dive + Simulator

### 背景

GPU服务器仍然不可达。继续本地学习+工具开发。成本优化=AI生产的终极约束!

### AI Serving Cost Optimization Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-serving-cost-optimization-deep-dive.md`，8个主题:

1. **Token Economics**: RTX 4090 INT4=$0.03/1M tok vs A100 cloud=$0.45 → 15x cheaper!
2. **GPU利用率**: BF16 B=1利用率0.5% → INT4 B=118利用率37.2% → cost↓72x!
3. **Cloud vs On-Premise**: RTX 4090 3yr TCO=$2,784 vs A100 cloud=$108,048 → 39x!
4. **量化成本节省**: INT4→6.7x cost savings → 但fused kernel必需!
5. **Autoscaling**: sleep/wake=30x cheaper per scale event vs HPA cold start!
6. **能效与碳成本**: INT4=7x能效→碳成本↓7x → 绿色AI=量化AI!
7. **RTX 4090经济学**: 本地推理最优=$0.015-0.03/1M tok → 15-30x cheaper!
8. **7 Core Laws**: Utilization-Leverage/Quantization-Cost/Local-Advantage/Sleep-Wake/Prefix-Sharing-Cost/Spot-GPU-Sleep-Wake/Energy-Carbon

### AI Serving Cost Optimization Simulator (新工具)

`tools/ai_serving_cost_optimization_simulator.py` 6个模拟器类:

- **TokenEconomicsSimulator**: 6配置×cost/1M tok → RTX 4090 INT4=$0.029 → 20x cheaper!
- **GPUUtilizationSimulator**: 5策略×利用率+成本 → INT4+INT8KV→37.2%利用率→72.6x cost↓
- **CloudVsOnPremiseSimulator**: 9 TCO对比 → RTX 4090 3yr=$3,373 vs A100 cloud=$107,748
- **QuantizationCostSimulator**: 5量化级别×batch+throughput+cost → INT4最优
- **AutoscalingCostSimulator**: HPA vs sleep/wake → 30x cheaper per scale event
- **EnergyEfficiencySimulator**: 6配置×能效+碳 → INT4=11 tok/s/W vs BF16=1.6 → 7x能效!

关键洞察: **RTX 4090本地推理=极致成本优化 → 15-30x cheaper than cloud → INT4量化是核心杠杆 → sleep/wake比HPA成本低30x → 绿色AI=量化AI**