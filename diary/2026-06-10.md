# 2026-06-10 日记

## 10:00 — RAG Systems Deep Dive + Simulator

### 背景

GPU服务器仍然不可达(SSH超时)。继续在本地推进理论学习+模拟器工具开发。

### RAG Systems Deep Dive (新笔记)

创建了 `notebook/fundamentals/rag-systems-deep-dive.md`，覆盖9个主题:

1. **RAG范式演进**: RAG-1.0(固定chunk)→RAG-2.0(hybrid search)→RAG-3.0(Self-RAG/CRAG/GraphRAG)
2. **Embedding Models**: BGE/D=1024, Matryoshka(前缀截断→D=256→98%recall→75%存储省), 量化FP16→99.5%/INT8→97-99%/Binary→90-95%+rerank→97%
3. **Chunking Strategies**: 5种(Fixed/Recursive/Semantic/Document/Agentic), Anthropic Contextual Chunking(加prefix→+5%recall), Goldilocks Zone=256-512
4. **Vector Database**: FAISS GPU→250x faster, HNSW=生产首选(M=16/ef=100→recall 95%), IVF+PQ=存储10x省
5. **Hybrid Search**: Dense+BM25+RRF(k=60)→互补→+5-10% recall, Reranking(cross-encoder→+5% recall), ColBERT(token-level→+7%但存储100x)
6. **Self-RAG/CRAG/GraphRAG**: Self-RAG=[Retrieve]/[IsREL]/[IsSUP] token→自适应, CRAG→web search fallback, GraphRAG→KG→全局推理
7. **RAG Serving Architecture**: 单GPU全栈(4.83GB→可行!), 分层(生产最优), Semantic Cache(30-50%命中→吞吐↑30-50%), Prefix Sharing(RAG天然适合!)
8. **RAGAS Evaluation**: Faithfulness > Recall > Precision(防幻觉最重要!), 高faithfulness低recall→RAGAS 0.833 > 高recall低faithfulness→0.741
9. **RTX 4090 RAG**: 7B INT4+BGE FP16+FAISS IVF+PQ→4.83GB→24GB可行, pipeline 47.5ms(cache miss)/27.5ms(fast)/6ms(cache hit)

### RAG Simulator (新工具)

创建了 `tools/rag_system_simulator.py`，5个模拟器类:

- **EmbeddingModel**: 量化impact(FP16=99.5%/INT8=100%/Binary=79.8% quality), Matryoshka截断, Binary+FP16 rerank hybrid
- **ChunkingSimulator**: 4策略(Fixed/Recursive/Semantic/Contextual)×4 chunk_size, Goldilocks Zone验证
- **HybridSearchSimulator**: Dense/BM25/RRF/Linear fusion+Cross-encoder reranking→7.4x improvement
- **RAGEvaluationSimulator**: RAGAS 4维度+Faithfulness-First验证
- **RTX4090RAGServingModel**: Pipeline latency+throughput+memory budget分析

关键结果:
- Contextual chunking=0.934最高recall(Anthropic方法+5%!)
- Faithfulness-First: 98% faithfulness → RAGAS 0.833 > 95% recall but 60% faithfulness → 0.741
- RTX 4090: 1M docs INT8→6.7GB→fits 24GB, throughput 21-37 QPS(with cache)
- RAG overhead=27.5ms(+138%)→但recall↑20%+幻觉↓50%

### RAG Core Laws

1. Retrieval-Quality Law: answer质量 ∝ 检索质量
2. Faithfulness-First Law: 防幻觉 > 高recall
3. Chunk-Goldilocks Law: chunk_size=256-512→recall peaks
4. Hybrid-Supplement Law: Dense+Sparse互补→+5-10%
5. Cache-Amplification Law: semantic cache→30-50% throughput↑
6. Separation-Law: 分层独立优化→最优

## 14:00 — Diffusion Models Deep Dive + Simulator

### Diffusion Models Deep Dive (新笔记)

创建了 `notebook/fundamentals/diffusion-models-deep-dive.md`，覆盖9个主题:

1. **扩散范式演进**: DDPM→DDIM→Flow Matching→Rectified Flow→Consistency Models 4代
2. **DDPM**: score function→噪声预测ε→推理1000步→太慢但质量好
3. **DDIM**: 确定性ODE→跳步加速→50步→同一训练不同采样
4. **Flow Matching**: 向量场v而非score→OT-CFM→最短路径→SD3用!
5. **Rectified Flow**: Reflow→路径变直→1-4步→SD3/Flux核心→2025生产首选!
6. **Consistency Models**: 自一致性→1步→CD蒸馏→但质量天花板低
7. **Architecture**: U-Net→DiT→MMDiT→Video DiT → Transformer统一→scaling law类似LLM
8. **Latent Diffusion**: VAE 48x压缩→latent空间diffusion→VAE decode瓶颈!
9. **RTX 4090**: DiT compute-bound(≠LLM decode) → VAE memory-bound → 大图瓶颈!

### Diffusion Simulator

`tools/diffusion_model_simulator.py` 4个模拟器:
- SD3-like 2B 4-step RF: **0.51s on RTX 4090** → 实时!
- VAE decode瓶颈: 768²=60% / 1024²=88% → VAE dominates!
- Trajectory straightness: DDPM curvature=0.5→100步 / RF=0.005→4步 → 直度决定速度!
- DiT-8B→OOM(49.7GB) / 2B→fit / INT8 8B→16GB→可行

### Diffusion Core Laws

1. Step-Quality Law: RF 4步≈DDIM 50步 → 12.5x加速
2. Latent-Space Law: 48x压缩 → 必需品
3. Trajectory-Straightness Law: 直度决定步数 → RF→4步
4. Architecture-Scaling Law: DiT类似LLM → 可预测
5. Compute-Bound Law: DiT=compute-bound → ≠LLM decode
6. VAE-Bottleneck Law: VAE=memory-bound → 大图瓶颈

## 20:00 — vLLM PR #45148 (NIXL KV connector metrics documentation)

### 背景

修复之前PR #44439的push失败问题(旧fork落后upstream→rebase冲突→数百merge conflicts)。

### 解决方案

- `git rebase --abort` 清理broken rebase状态
- 从最新upstream main创建fresh branch `doc/nixl-kv-connector-metrics-v2`
- 手动重新apply 2处文档改动(metrics.py docstrings + disagg_prefill.md metrics section)
- 用auth token via gh-proxy.com成功push到Jackie2049/vllm fork
- 删除旧branch `doc/nixl-kv-connector-metrics`
- 创建新PR #45148: https://github.com/vllm-project/vllm/pull/45148

### 文档改动内容

1. **metrics.py**: KVConnectorLogging class docstring(observe→aggregate→reduce→log pipeline), observe()和log()方法docstrings(TP>1时stats pre-aggregated semantics)
2. **disagg_prefill.md**: "Interpreting KV Transfer Metrics" section, 含3列表格(metric/semantics/TP>1 note) + tip(per-rank throughput×TP degree=system throughput)

### GPU状态

高校服务器(219.223.198.62)仍然不可达(SSH超时)。

## 22:00 — Distributed Systems Deep Dive Extended + Simulator

### 背景

扩展已有的distributed-systems-deep-dive.md, 添加6个新章节(7-12)和simulator工具。GPU仍不可用。

### 新增章节

- **§7 Failure Detection**: φ-accrual vs timeout → φ-accrual 0% FP rate vs timeout 0.19-55.7% → 自适应检测远优!
- **§8 Clock & Ordering**: Lamport(partial)→Vector(complete causal)→DynamoDB用vector clocks→AI Infra用step number(≈Lamport)
- **§9 Replication & Leader Election**: Primary-Backup/Chain/Quorum + Raft/Bully/Ring算法 + split brain prevention
- **§10 Distributed Storage**: etcd(CP/Raft→Kubernetes/Ray GCS) vs Redis(AP/sharded→feature store/model cache)
- **§11 PACELC Theorem**: 扩展CAP → P(A/C) + E(L/C) → Training=PC/EC → Inference=PA/EL → Checkpoint=PC/EC
- **§12 Core Laws 7-10**: Detection/Ordering/Replication-Choice/PACELC定律

### Distributed Systems Simulator (新工具)

`tools/distributed_systems_simulator.py` 5个模拟器类:
- **ConsensusSimulator**: Raft/Paxos/BFT对比 → 5节点0-3故障 → Raft可选举/Paxos达成共识/BFT需3f+1
- **FailureDetectorSimulator**: Timeout vs φ-accrual → φ-accrual **0% FP** vs timeout 0.19-55.7% → 生产推荐φ-accrual!
- **ClockOrderingSimulator**: Lamport vs Vector → Lamport不能检测并发/Vector可以→AI Infra用step number够了
- **ReplicationSimulator**: Primary-Backup/Chain/Quorum → 训练=PB(coordinator)/推理=W=1/R=1/PD=Chain
- **PACELCSimulator**: Training=PC/EC/Inference=PA/EL/PD=PA/EL/Checkpoint=PC/EC

关键洞察: **AI Infra不是单一PACELC选择 → 训练=一致性 / 推理=可用性 / 不同组件不同需求!**

### vLLM PR更新

- PR #45148 DCO修复: 添加Signed-off-by行 → force push → DCO应通过
- PR状态: OPEN, mergeable, ReadTheDocs preview生成中

## 23:30 — AI Serving Observability Deep Dive + Simulator

### 背景

可观测性是AI Infra从开发到生产的关键桥梁。GPU仍不可用。

### AI Serving Observability Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-serving-observability-deep-dive.md`，7个主题:

1. **Metrics系统**: vLLM V1 Prometheus (gpu_cache_usage, num_requests_running, TTFT/ITL histogram, prefix_cache_hit_rate, KV connector stats)
2. **结构化日志**: vLLM当前非JSON → 生产应JSON → request_id关联
3. **Distributed Tracing**: OpenTelemetry → vLLM尚未集成 → 未来关键改进
4. **SLO/SLA管理**: 4核心SLI (TTFT<500ms P99, ITL<100ms P99, throughput>1K tok/s, availability 99.9%)
5. **Autoscaling**: HPA vs sleep/wake → sleep/wake 30x更快 → RTX 4090推荐sleep/wake
6. **Production Debugging**: 5常见问题 (OOM/高延迟/GPU故障/低吞吐/请求drop) + 诊断流程
7. **Core Laws**: Observability/SLO-Driven/Cache-Utilization/Cold-Start/Debugging-Hierarchy/Error-Budget/Prefix-Visibility

### AI Serving Observability Simulator (新工具)

`tools/ai_serving_observability_simulator.py` 4个模拟器:
- **MetricsSimulator**: vLLM metrics → TTFT P99=220ms, ITL P99=10ms, KV=95%
- **SLOSimulator**: SLO合规检查 → TTFT/ITL通过但availability 99%未达99.9%
- **AutoscalingSimulator**: HPA vs sleep/wake → sleep/wake 30x快 → Error budget第10天耗尽
- **IncidentResponseSimulator**: 4种incident+runbook → OOM MTTR≈14min/GPU故障≈130min

关键洞察: **可观测性=生产必要条件 → 无监控=盲飞 → SLO驱动运维 → sleep/wake比HPA更适合AI serving**

### vLLM Issue #33267调研

发现已有PR #34062/#36244(均closed) → 可能LayerName hoisting(torch>=2.11)已解决核心问题 → 留了澄清问题

## 01:00 — AI Data Flow Architecture Deep Dive + Simulator

### 背景

GPU服务器仍然不可达。继续本地学习+工具开发。

### AI Data Flow Architecture Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-data-flow-architecture-deep-dive.md`，7个主题:

1. **Inference数据流**: vLLM V1 7步路径(HTTP→Tokenizer→Scheduler→ModelRunner→GPU→Sampling→Response)
2. **Training数据流**: verl RL 14步(Dataset→Forward→Reward→Backward→Optimizer→Checkpoint)
3. **Serialization格式**: SafeTensors(快+安全) vs pickle(慢+不安全) → 生产必须SafeTensors!
4. **Checkpoint管理**: 同步barrier(一致但慢) vs 异步+版本管理(verl=最优)
5. **Model Registry**: HF Hub+safetensors → vLLM HF download→load→serve → 简单!
6. **Feature Store**: online(Redis/AP) vs offline(HDFS/CP) → Feast统一 → 双写一致性挑战
7. **7 Core Laws**: Incremental-Transfer/Serialization-Cost/Compression-Ratio/Checkpoint-Consistency/Transfer-Bottleneck/Feature-Store-Dual-Write/Data-Flow-Debugging

### AI Data Flow Simulator (新工具)

`tools/ai_data_flow_simulator.py` 6个模拟器类:

- **InferenceDataFlowSimulator**: 7步推理路径+增量传输(>90%通信量节省)+TTFT/ITL估算
- **SerializationSimulator**: 5格式对比(PyTorch/SafeTensors/GGUF/FP8/BF16) → SafeTensors最优
- **CheckpointSimulator**: 3策略(sync/async/async+versioning) → verl异步+版本管理=最优
- **TransferBottleneckSimulator**: 6场景(PCIe/NVLink/Ethernet/HBM) → INT4缓解PCIe瓶颈
- **FeatureStoreSimulator**: online vs offline → 500x速度差距 → 双写一致性lag=5s
- **DataFlowDebuggingSimulator**: 4层debugging+5场景 → 平均27min解决 → metrics最快

关键洞察: **增量传输>90%通信节省 → safetensors是生产必需 → INT4量化=75%省但需fused kernel → verl异步checkpoint=最优**

### vLLM PR更新

- 旧PR #45148被意外关闭 → 创建新PR #45157 → OPEN, MERGEABLE

## 03:00 — AI Serving Cost Optimization Deep Dive + Simulator

### 背景

GPU服务器仍然不可达。继续本地学习+工具开发。成本优化=AI生产的终极约束!

### AI Serving Cost Optimization Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-serving-cost-optimization-deep-dive.md`，8个主题:

1. **Token Economics**: RTX 4090 INT4=$0.03/1M tok vs A100 cloud=$0.45 → 15x cheaper!
2. **GPU利用率**: BF16 B=1利用率0.5% → INT4 B=118利用率37.2% → cost↓72x!
3. **Cloud vs On-Premise**: RTX 4090 3yr TCO=$2,784 vs A100 cloud=$108,048 → 39x!
4. **量化成本节省**: INT4→6.7x cost savings → 但fused kernel必需!
5. **Autoscaling**: sleep/wake=30x cheaper per scale event vs HPA cold start!
6. **能效与碳成本**: INT4=7x能效→碳成本↓7x → 绿色AI=量化AI!
7. **RTX 4090经济学**: 本地推理最优=$0.015-0.03/1M tok → 15-30x cheaper!
8. **7 Core Laws**: Utilization-Leverage/Quantization-Cost/Local-Advantage/Sleep-Wake/Prefix-Sharing-Cost/Spot-GPU-Sleep-Wake/Energy-Carbon

### AI Serving Cost Optimization Simulator (新工具)

`tools/ai_serving_cost_optimization_simulator.py` 6个模拟器类:

- **TokenEconomicsSimulator**: 6配置×cost/1M tok → RTX 4090 INT4=$0.029 → 20x cheaper!
- **GPUUtilizationSimulator**: 5策略×利用率+成本 → INT4+INT8KV→37.2%利用率→72.6x cost↓
- **CloudVsOnPremiseSimulator**: 9 TCO对比 → RTX 4090 3yr=$3,373 vs A100 cloud=$107,748
- **QuantizationCostSimulator**: 5量化级别×batch+throughput+cost → INT4最优
- **AutoscalingCostSimulator**: HPA vs sleep/wake → 30x cheaper per scale event
- **EnergyEfficiencySimulator**: 6配置×能效+碳 → INT4=11 tok/s/W vs BF16=1.6 → 7x能效!

关键洞察: **RTX 4090本地推理=极致成本优化 → 15-30x cheaper than cloud → INT4量化是核心杠杆 → sleep/wake比HPA成本低30x → 绿色AI=量化AI**

## 04:30 — GPU Memory Management Deep Dive + Simulator

### 背景

GPU服务器仍然不可达。继续本地学习+工具开发。GPU内存管理=推理核心!

### GPU Memory Management Deep Dive (新笔记)

创建了 `notebook/fundamentals/gpu-memory-management-deep-dive.md`，8个主题:

1. **CUDA Memory Hierarchy**: Register→Shared→L2→HBM→Host, RTX 4090大L2=72MB→低occupancyOK
2. **PagedAttention**: BlockPool+FreeKVCacheBlockQueue+BlockHashToBlockMap → 预分配→slice→3.9x faster→零碎片
3. **Memory Budget**: BF16 B=22→INT4+INT8KV B=118→6x并发 → 量化=核心杠杆
4. **Eviction & Preemption**: swap快4-44x→vLLM默认recompute→RTX 4090可能需修正
5. **vLLM V1源码**: BlockPool(kv_cache_manager.py)+KVCacheManager+BlockHashToBlockMap(1:N)
6. **Memory Pooling**: 预分配→pool slice→3.9x faster→零碎片 → vs动态malloc
7. **Multi-Group KV**: MLA/MoE/sliding window → 不同block策略
8. **7 Core Laws**: Memory-Bound/PagedAttention/Prefix-Caching/Recompute-vs-Swap/Pre-allocation/Budget/StreamingLLM-Fixed

### GPU Memory Management Simulator (新工具)

`tools/gpu_memory_management_simulator.py` 6个模拟器类:

- **CUDAMemoryHierarchySimulator**: 5层级性能对比 → Register 400x faster than HBM
- **PagedAttentionBlockSimulator**: block分配+prefix caching+碎片率 → 碎片率1%→近零!
- **MemoryBudgetSimulator**: 5配置×并发+吞吐 → INT4+INT8KV B=118→6x并发!
- **EvictionPreemptionSimulator**: recompute vs swap vs StreamingLLM → swap快44x
- **AllocationPatternSimulator**: 4模式×分配速度+碎片 → PA=4x faster+1%碎片
- **StreamingLLMSimulator**: 固定168MB→无限对话→无OOM

关键洞察: **PagedAttention=零碎片+O(1) → swap快44x → StreamingLLM=固定168MB无OOM → 预分配3.9x faster**

## 06:00 — AI Infrastructure Security Deep Dive + Simulator

### 背景

安全=AI基础设施底层需求。补齐安全维度知识。

### AI Infrastructure Security Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-infrastructure-security-deep-dive.md`，7个主题:

1. **Secure Inference**: prompt injection(85%→FSM→0.85%), adversarial, model extraction, data leakage
2. **Secure Training**: data poisoning, model theft, training integrity, privacy(DP-SGD)
3. **Secure Deployment**: container security, API security, network security
4. **Supply Chain**: HF权重+pip+conda+Docker+NVIDIA driver → 签名+哈希+验证
5. **GPU Infrastructure**: ECC差异(RTX 4090无ECC→A100有), 侧信道, 驱动安全
6. **Security Ops**: 监控+响应+合规(GDPR/EU AI Act/SOC2)
7. **7 Core Laws**: Input-Validation/Isolation/Supply-Chain-Trust/No-ECC/DP-Privacy/Rate-Limit/Security-Depth

### AI Infrastructure Security Simulator (新工具)

`tools/ai_infrastructure_security_simulator.py` 6个模拟器类:

- **PromptInjectionSimulator**: 3攻击×5防御 → FSM→99% blocked→<5% overhead
- **ModelExtractionSimulator**: 4方法×成本+fidelity → rate limit→extraction不可行
- **DataPoisoningSimulator**: 6投毒率×4防御 → 组合→95%检测
- **SupplyChainSimulator**: 6组件×验证 → verified→90% reduction
- **DPPrivacySimulator**: ε sweep → ε=8→GDPR合规→accuracy↓<1%→推荐
- **SecurityDepthSimulator**: 4层×覆盖 → 全层→95%覆盖→3.5% overhead

关键洞察: **FSM→99% injection防御<5% overhead → ε=8 DP-privacy GDPR合规 → 多层→95%覆盖 → 安全=深度防御**

## 07:30 — Compiler Optimizations for AI Inference Deep Dive + Simulator

### 背景

编译优化=AI推理的隐形加速器。结合RTX 4090实测数据深入分析。

### Compiler Optimizations Deep Dive (新笔记)

创建了 `notebook/fundamentals/compiler-optimizations-ai-inference-deep-dive.md`，7个主题:

1. **torch.compile**: Dynamo→FX Graph→Inductor→Triton → B=1 4.09x但B≥4 0.80x → Triton GEMM慢!
2. **CUDA Graph**: 消除launch overhead → 7B仅1.05x → 但P99稳定 → 生产=稳定jitter
3. **Kernel Fusion**: 5模式(SiLU/RMSNorm/QKV/FlashInfer/AWQ) → FlashInfer 15.72x最大!
4. **Memory Planning**: DCE+常量折叠+内存复用+in-place+view vs copy → PagedAttention类似
5. **vLLM编译策略**: FlashInfer+cuBLAS+CUDA Graph → 不用torch.compile → 专用kernel更优!
6. **JIT vs AOT**: AOT(CUTLASS/FlashInfer)0.1-0.5s vs JIT(torch.compile)30s → 生产=AOT!
7. **7 Core Laws**: Python-Overhead/Fusion-Launch/Triton-vs-cuBLAS/CUDA-Graph-Jitter/AOT-vs-JIT/Quantization-Fusion/Compile-Decision

### Compiler Optimizations Simulator (新工具)

`tools/compiler_optimizations_simulator.py` 6个模拟器类:

- **PythonOverheadSimulator**: 8 batch size×overhead占比 → B=1 71%→compile 4.09x → B≥4 <10%
- **KernelFusionSimulator**: 5融合case×speedup → FlashInfer 15.72x → AWQ/TE 3.70x
- **CUDAGraphSimulator**: 5 batch×launch占比 → 7B=1.05x → 小模型2.43x → 稳定jitter
- **TritonVsCuBLASSimulator**: 5 ops×Triton vs cuBLAS → RMSNorm 2.75x → GEMM 0.64x
- **CompileDecisionSimulator**: 6场景×推荐 → 推理=FlashInfer → 训练B≤4=compile
- **AOTvsJITSimulator**: 6编译方式×warmup → AOT 0.1s vs JIT 30s → 生产=AOT!

关键洞察: **推理=FlashInfer+cuBLAS+CUDA Graph → 不需要torch.compile → Triton仅赢reduction → cuBLAS赢GEMM → CUDA Graph=稳定jitter而非加速**

## 09:00 — Network & Communication Optimization Deep Dive + Simulator

### 背景

网络=分布式AI系统的生命线。从TCP到RDMA，从NCCL到DeepEP，分析网络通信瓶颈。

### Network & Communication Optimization Deep Dive (新笔记)

创建了 `notebook/fundamentals/network-communication-optimization-deep-dive.md`，7个主题:

1. **Network Stack**: TCP/RDMA/RoCE/InfiniBand/NVLink/PCIe → RDMA=零拷贝+kernel bypass→10x latency↓
2. **NCCL**: Ring/Tree拓扑→自动选择→AllReduce 2.76GB/s(RTX 4090)→FSDP灾难
3. **KV Transfer**: NCCL/RDMA/NixlConnector/DeepEP V2 → PCIe 32MB→3ms→仅3%TTFT→PD可行!
4. **GPU Direct RDMA**: 零CPU拷贝→GPU→NIC→GPU→30-40x加速→RTX 4090不支持!
5. **Network Topology**: 单节点NVLink→小集群Ethernet→大集群IB→RTX 4090=PCIe only→单GPU最优
6. **Comm-Compute Overlap**: 计算>通信→overlap有效 / 通信>计算→无效→RTX 4090=后者
7. **7 Core Laws**: PCIe-Disaster/RDMA-Zero-Copy/NCCL-Ring-Tree/KV-Transfer-PCIe/Overlap-Effective/Network-Cost/Local-Inference-Wins

### Network & Communication Optimization Simulator (新工具)

`tools/network_communication_optimization_simulator.py` 6个模拟器类:

- **NetworkStackSimulator**: 5协议×6数据量 → NVLink 300GB/s vs PCIe 12GB/s → 25x差距
- **NCCLCollectiveSimulator**: AllReduce+FSDP scaling → 7B 8GPU PCIe comm_ratio=99.4%
- **KVTransferSimulator**: 4方法×6KV大小+DeepEP V2 → NVLink 0.3%TTFT vs PCIe 14%TTFT
- **GPUDirectRDMASimulator**: 传统vs GPU Direct → 40x加速(消除4次CPU memcpy)
- **NetworkTopologySimulator**: 4拓扑+决策树 → RTX 4090=$120/GPU vs IB集群=$20K/GPU
- **CommComputeOverlapSimulator**: 5场景×overlap效率 → NVLink>90% vs PCIe<32%

关键洞察: **PCIe=多GPU灾难(0.46x) → NVLink=109x差距 → KV transfer PCIe可行(3%TTFT) → RDMA=零拷贝10x → overlap需要compute>comm → RTX 4090=单GPU推理最优**

## 11:00 — AI Inference Batching & Scheduling Deep Dive + Simulator

### 背景

批处理+调度=推理吞吐的灵魂。GPU仍不可用，继续本地学习。

### AI Inference Batching & Scheduling Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-inference-batching-scheduling-deep-dive.md`，8个主题:

1. **Batching Strategies**: No→Static→Continuous→Chunked 4代; B=1→79tok/s / B=118→4,791 → 60x!
2. **Token Budget**: vLLM V1 unified budget→decode优先→prefill用剩余→每步动态分配
3. **Scheduling Algorithms**: FCFS/SJF/Priority/Hybrid→生产推荐Hybrid(Priority+FCFS)
4. **Prefill Scheduling**: chunked prefill→chunk=512→ITL↑32%可接受 / chunk=2048→ITL↑326%不可
5. **Decode Scheduling**: B↑→ITL↑→SLO约束→最优B=32-55→ITL≤15ms→4,190 tok/s
6. **KV Cache Pressure**: 量化KV→并发↑6x→INT4+INT8KV=必需→BF16并发2→INT4并发118
7. **SLO-Aware**: ITL-first+throughput-limit→B≤55→全部SLO合规→生产最优
8. **7 Core Laws**: Batch-Throughput/Decode-First/Chunk-Prefill/KV-Pressure/Long-Context-Zero-Sum/SLO-Balance/Prefix-Sharing-Batching

### AI Inference Batching & Scheduling Simulator (新工具)

`tools/ai_inference_batching_scheduling_simulator.py` 5个模拟器类:

- **BatchingStrategySimulator**: 4代批处理×throughput+padding+ITL → Continuous B=32→2,468tok/s→最优!
- **TokenBudgetSimulator**: budget分配→decode优先→剩余给prefill→118 decode→仅10tok给prefill
- **SchedulingAlgorithmSimulator**: FCFS/SJF/Priority/Hybrid对比 → Hybrid防饥饿→推荐
- **KVPressureSimulator**: BF16→2并发(灾难!)/INT4+INT8KV→118→6x! / StreamingLLM→130→无限!
- **SLOAwareSimulator**: 4配置×SLO合规 → B=55→全部合规→B=118→availability不合规!

关键洞察: **B↑→吞吐↑60x但ITL↑→SLO约束→最优B=32-55→decode优先→chunk=512→INT4+INT8KV=并发必需→StreamingLLM→无限对话→最稳**

## 13:00 — AI Request Routing & Endpoint Optimization Deep Dive + Simulator

### 背景

请求路由=AI serving的入口守门人。从负载均衡到SLA路由，从API设计到circuit breaker，路由=用户→GPU的桥梁。

### AI Request Routing & Endpoint Optimization Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-request-routing-endpoint-optimization-deep-dive.md`，9个主题:

1. **Load Balancing**: Round Robin/Least Connections/Least Load/Prefix Hash/Hybrid → Hybrid(prefix+least load)最优
2. **SLA-Aware Routing**: Premium→专用GPU→99.99%→Standard→共享→99.9%→Free→高负载→99.5%
3. **API Endpoint Design**: REST→gRPC→WebSocket→SSE→HTTP/3 → vLLM=SSE+REST→推荐
4. **Rate Limiting**: Token Bucket→burst+长期→GPU-aware动态→推荐
5. **Circuit Breaker**: 3态(closed/open/half-open)→防级联→30s冷却→恢复
6. **Graceful Degradation**: 4级→限流→减功能→降模型→拒绝→逐步不跳
7. **Timeout & Retry**: 分级timeout→2-3 retry→exponential backoff→jitter→幂等
8. **Health Check**: liveness≠readiness→3级→startup 140s→warmup 5s
9. **7 Core Laws**: Least-Load/SLA-Tier/Token-Rate/Circuit-Breaker/Degradation/Timeout-Backoff/Health-Readiness

### AI Request Routing & Endpoint Optimization Simulator (新工具)

`tools/ai_request_routing_endpoint_optimization_simulator.py` 6个模拟器类:

- **LoadBalancingSimulator**: 5策略×1000请求 → Hybrid: prefix 302+least load → latency=13.5ms最优!
- **SLARoutingSimulator**: 3 Tier × SLO合规 → Premium ITL=7.9ms < 50ms SLO → YES
- **RateLimitingSimulator**: 3桶配置+自适应 → balanced: burst=10 refill=55 → 推荐
- **CircuitBreakerSimulator**: 3态+5分钟timeline → closed 238s / open 60s → availability 79.3%
- **GracefulDegradationSimulator**: 4级+触发+恢复 → L1→B=32 / L3→降模型→2x吞吐↓10%质量
- **HealthCheckSimulator**: 启动140s+运行+4 GPU指标 → readiness 52.7%→短暂不可→保护

关键洞察: **Hybrid路由→prefix hash 84%KV省+least load均衡 → SLA分级→Premium→专用→99.99% → Token bucket→burst+长期平衡 → 熔断→防级联 → 4级降级→逐步恢复 → 140s启动→readiness≠liveness**

## 15:00 — AI Training Infrastructure Deep Dive + Simulator

### 背景

训练基础设施=AI模型诞生的工厂。从training loop到DP internals，从gradient accumulation到mixed precision，分析训练系统瓶颈。

### AI Training Infrastructure Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-training-infrastructure-deep-dive.md`，8个主题:

1. **Training Loop**: forward→backward→optimizer→标准循环→7B=70GB→5x参数→OOM!
2. **Data Parallelism**: DDP/FSDP/Hybrid→DDP简单但OOM→FSDP内存省8x但PCIe通信灾难
3. **Gradient Accumulation**: micro×accum=等效B→小GPU→大batch桥梁→DDP no_sync省通信
4. **Mixed Precision**: BF16=最优→1.51x+49%省→FP16 AMP反而慢!→FP8 TE B≥4
5. **Checkpointing**: 同步→暂停→浪费/异步→无暂停→verl版本管理→最优
6. **Training Pipeline**: backward=55-62%→主导→PCIe 7B 8GPU→99.4%通信→0.46x灾难!
7. **RTX 4090训练**: ≤2GPU小模型→可行 / 7B LoRA→单GPU→可行 / 7B全→NVLink必需→不适合!
8. **7 Core Laws**: Training-Memory-5x/PCIe-Disaster/BF16-Best/Gradient-Accumulation/LoRA-Feasibility/Async-Checkpoint/Training-Decision

### AI Training Infrastructure Simulator (新工具)

`tools/ai_training_infrastructure_simulator.py` 6个模拟器类:

- **TrainingLoopMemorySimulator**: 7配置×内存 → 7B BF16=98GB OOM / FSDP8=12.25GB fits / LoRA=0.39GB fits!
- **DataParallelismSimulator**: 3模型×3GPU数×PCIe/NVLink → 7B 8GPU PCIe=0.3x / NVLink=7.16x
- **GradientAccumulationSimulator**: 4配置×等效B+内存 → B=4×8→72GB→OOM→LoRA→不需要accum!
- **MixedPrecisionSimulator**: 4精度×speedup+内存 → BF16=1.51x / FP16 AMP=0.85x慢! / FP8=1.48x
- **CheckpointingSimulator**: 5策略对比 → async+version=最优 / FSDP+no grad_ckpt=0.478GB→更小!
- **TrainingPipelineSimulator**: 4场景×瓶颈 → PCIe→99.4%comm→0.46x / LoRA→0%comm→单GPU最优

关键洞察: **训练内存=5x参数→7B=70GB→RTX 4090 OOM → PCIe训练灾难→0.46x → BF16=最优训练精度 → LoRA→单GPU可行→verl GRPO → 小模型≤2GPU / 7B全参数→NVLink必需→RTX 4090不适合**

## 17:00 — AI Model Deployment & Production Deep Dive + Simulator

### 背景

模型部署=AI从实验到生产的最后一步。从safetensors到容器化，从CI/CD到版本管理，分析部署生产化。

### AI Model Deployment & Production Deep Dive (新笔记)

创建了 `notebook/fundamentals/ai-model-deployment-production-deep-dive.md`，8个主题:

1. **Model Packaging**: pickle→不安全+慢 / safetensors→安全+2x快+mmap / GGUF→量化内置 / ONNX→TensorRT
2. **Containerization**: nvidia-docker→GPU / 多阶段构建→3x省 / sleep/wake→1s→vs HPA 30s→30x!
3. **CI/CD**: 模型验证→容器构建→集成测试→部署→监控 → 5-10min CI → 2-3min CD
4. **Model Versioning**: HF Hub+HF-Mirror→中国10x快 / MLflow→完整生命周期 / K8s rollback→1命令
5. **Deployment Strategies**: Rolling→0 downtime 1x / Blue-Green→2x资源最安全 / Canary→渐进→大规模
6. **A/B Testing**: Shadow→零风险→离线→A/B→5%→渐进 → z=1.885→2%差异不显著→需5000样本!
7. **Production Lifecycle**: Dev→Staging→Canary→Prod→Monitor→Retire → 6阶段→43天→$433.5
8. **7 Core Laws**: SafeTensors-Over-Pickle/Single-GPU-Container/Multi-Stage-Build/Rolling-Deploy/Version-Rollback/Shadow-Eval/Production-6-Phase

### AI Model Deployment & Production Simulator (新工具)

`tools/ai_model_deployment_production_simulator.py` 5个模拟器类:

- **ModelPackagingSimulator**: 4格式×4模型下载 → SafeTensors 2.1x快 / GGUF 3.1x / HF-Mirror 10x快!
- **ContainerizationSimulator**: 3镜像+4启动对比 → Multi-Stage 6.6GB vs 24GB → sleep/wake=1s!
- **DeploymentStrategiesSimulator**: 3策略×downtime+resource+risk → Rolling=1x→推荐!
- **ABTestingSimulator**: 2模型+A/B+Shadow → z=1.885不显著→Shadow→零风险→第一步!
- **ProductionLifecycleSimulator**: 6阶段+成本 → Dev→Staging→Canary→Prod→$433.5/43天

关键洞察: **safetensors→安全+2x快+mmap→生产必需 → sleep/wake=1s→vs HPA 30s→30x → Rolling→最简单部署 → Shadow→零风险评估→第一步 → 6阶段生命周期→逐步验证 → RTX 4090→单GPU容器→rolling→最优**