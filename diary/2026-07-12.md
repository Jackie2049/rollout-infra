# 2026-07-12 Diary (Session 5)

## Summary
DeepSpeed AutoEP+AutoTP folding deep dive (+4066/-85), SGLang RolloutKV prefix pinning (+768/-5), cross-framework attention NaN pattern analysis, PyTorch PR #184119 P9 Fusion Guard validation, cross-framework tool-calling pipeline analysis. All 6 patches still awaiting user review. GPUs still offline.

## Work Done

### 1. DeepSpeed AutoEP+AutoTP Folding Deep Dive
- **PR #8064** (tohtana, +4066/-85): Parallel folding — AutoTP for dense path coexists with AutoEP for expert path on same ranks
- **Core innovation**: Cross-lane EP — EP no longer confined to DP subset. EP groups span TP lanes and dense-DP ranks
- **Invariant**: `tp * dp == ep * etp * edp == stage_size`
- **Gradient convention** (4 strategies): AVERAGE (router/dense), EXPERT_TP_CANCEL (routed experts), SKIP (TP-sharded), SUM (reserved SP)
- **EXPERT_TP_CANCEL critical**: `restore_combined` backward injects `tp_size` factor on expert gradients. Must divide out without TP all_reduce — invisible to Adam but real for SGD/Lion/Muon and gradient clipping
- **Dispatch**: `partition_assignments()` + `restore_combined()` replaces `combine_from_routed()` for folded TP
- **25 files**: 9 core implementation, 9 tests, 7 modified
- **Limitations**: NO DeepCompile, NO PP, NO SP, NO ZeRO-3, NO ETP>1, NO offload
- **Validation**: 8×H100 confirmed gradient parity, SGD-verified (not just Adam)
- **Bug #8102**: Python 3.9 `TypeError: unsupported operand type(s) for |` → CLOSED (#8119 merged)
- Deep reading saved: `notebook/projects/deepspeed-autoep-autotp-folding-deep-reading.md`

### 2. Cross-Framework Issue Scan
- **DeepSpeed Q3 Roadmap #8104**: AutoEP (Q3), TPU, OPD Trainer, DeepCompile passes, Ray PP, tuning guide, nightly CI
- **SGLang #30887**: NVFP4 gated MoE fails to load with TP=2+ on Gemma-4-26B-A4B — gate/up padding explicitly rejected
- **SGLang #30884**: DeepSeek V4 PDMux support — missing split_prefill entry point, topology API moved
- **SGLang #30875**: FlashInfer 0.6.14 breaks allreduce fusion — tilelang libcudart_stub.so vs real libcudart
- **vLLM #48364**: xpu_mla_sparse NaN — `e_max=-inf` running max pattern, fully-masked first chunk → `exp2(nan)` → permanent NaN. Same bug class as previous attention kernel NaN. Affects DSA prefill and fp8 decode.
- **vLLM #48347**: InternLM2 streaming tool parser loses arguments when tool call arrives in single delta (same bug class as #47907/#48294)
- **vLLM #48342**: DSv3 streaming tool parser truncates non-string arguments — hardcoded `"}` heuristic fails for numbers/booleans/nested objects
- **Megatron #5749**: EP deadlock with Gemma4 — alltoall token dispatcher conflicts with DP gradient reduce-scatter on same NCCL stream. Non-deterministic (534/584/719/852 steps). Confirmed: no EP alltoall = completes 1000 steps
- **TRL #6350**: Reward-hacking monitoring (Aarav500, same user as verl #7004 Flight Recorder). 0 maintainer response

### 3. SGLang RolloutKV Deep Dive
- **PR #28608** (+768/-5): Prefix KV cache pinning for RL rollout — proactive commit-and-pin replaces reactive RadixAttention
- **Mechanism**: `inc_lock_ref` moves node to `protected_size` (immune to LRU). TTL-based stale pin eviction for crash safety
- **Performance**: Logprob scoring **22-41x speedup** on Qwen3-32B/A100. Generation 1.25-1.65x
- **API**: `--enable-rollout-kv` server flag + custom_params (`rollout_kv_commit`, `rollout_kv_reuse_only`, etc.)
- **Reliability**: TTL eviction (default 600s) handles follower crash/OOM, idempotent unprotect
- **GRPO critical**: 22-41x logprob speedup directly accelerates training iterations; `rollout_kv_reuse_only` prevents rollout suffix pollution
- **Status**: OPEN, 0 human reviews (only Gemini bot quota comment)
- Deep reading saved: `notebook/projects/sglang-rolloutkv-prefix-pinning-deep-reading.md`

### 4. Cross-Framework Tool-Calling Pipeline Analysis
- **vLLM inference side**: Streaming tool parsers lose/corrupt tool calls on multi-token deltas (#48347 InternLM2, #48342 DSv3)
- **TRL training side**: VLM pixel_values incorrectly sliced during tool-call iteration (#6274 fix ready)
- **Missing tests**: Both bug types involve edge cases (single-delta streaming, grid-aware pixel slicing) that existing tests don't cover
- **Same bug class family**: #48347 is the 3rd instance after #47907 (gigachat3/granite) and #48294 (llama3_json/jamba)
- **Dead code in #48342**: `diff.encode(...).decode(...) if diff is str else diff` — `is str` compares against *type object* (always False)

### 5. Knowledge Map Updated
- Added `autoep_folding` domain (3 connections: cross-lane EP, gradient convention, route-full dispatch)
- Added aliases: autoep, folding, ep_tp, cross_lane
- Theory domains: 18 total
- Readiness: Theory 15/10, Infra 10/10, Math→Bug 10/10

### 6. rLLM Trainer Plugin Architecture Analysis
- **terminal-rl** branch is 46 commits ahead of main — active development branch
- **PR #742 MERGED**: `@rllm.register_adv_estimator` — first-class API for custom advantage estimators
- **PR #743 OPEN**: PKPO pass@k advantage estimator using new hook (arXiv:2505.15201)
- **PR #713 MERGED**: `@rllm.register_loss` — pluggable custom policy losses
- **#605 implications**: Our fix must be rebased to `terminal-rl` and rewritten as `@rllm.register_adv_estimator("grouped_grpo")` plugin, not a direct code change to `main`
- **6 built-in estimators**: grpo, reinforce, reinforce++_baseline, prpo, rloo, echo
- Analysis saved: `notebook/projects/rllm-trainer-plugin-architecture-analysis.md`

### Issue Status Updates
- rLLM #605: Still OPEN (41+ days), 0 comments — our fix on jackie2049/rllm fork still the only work
- TRL #6361: Still OPEN, 0 comments — Qwen3.5 template fix ready
- DeepSpeed #8102: CLOSED (Python 3.9 AutoEP bug fixed)
- Megatron #5749: Still OPEN — EP deadlock root cause confirmed
- 6 patches total (5 TRL + 1 rLLM) all awaiting user review/authorization

### GPU Status
- University: Connection timed out — still OFFLINE
- Matpool: Connection refused — still OFFLINE

## Notes Created
- `notebook/projects/deepspeed-autoep-autotp-folding-deep-reading.md` — AutoEP+AutoTP deep reading
- `notebook/projects/sglang-rolloutkv-prefix-pinning-deep-reading.md` — RolloutKV prefix pinning
- `notebook/projects/attention-nan-running-max-pattern-analysis.md` — Cross-framework attention NaN via -inf running max
- `notebook/projects/pytorch-184119-sm89-fp8-bf16-fusion-guard-analysis.md` — P9 validation
- `notebook/projects/rllm-trainer-plugin-architecture-analysis.md` — rLLM plugin hooks for #605

## Total Project Stats
- 445+ tools, 336+ project notes, 62 fundamentals notes, 1 synthesis
- Theory domains: 18 (added: autoep_folding)

---

# 2026-07-12 Diary (Session 4)

## Summary
Cross-framework issue scan (DeepSpeed Q3 Roadmap, Megatron CI/DSA, NanDetectMode stalled), created GRPO NaN Detective utility for RTX 4090 debugging. GPUs still offline.

## Work Done

### 1. Cross-Framework Issue Scan
- **DeepSpeed**: #8104 Q3 Roadmap (empty draft - AutoEP, DeepCompile, OPD Trainer); #8085 zero.Init partitioning spike (active discussion); #8103 stage3_partition_stream_chunk_size PR merged; #8092 Gefen-Muon (controversial, unvalidated claims)
- **Megatron**: #5749 EP deadlock Gemma4 (NEW); #5747 CI regression from TE 2.17 bump; #5732 parameter-level optimization design; #5717 MFSDPv2 CUDA graph; #5703 DSA fused backends (very active, 8+ comments, #5099 cudnn backend merged)
- **TRL**: #6350 reward-hacking monitoring (same user Aarav500 as verl #7004, Flight Recorder code, 0 maintainer response); #6274 other contributor claimed fix; #6361 still 0 comments
- **PyTorch**: #187653 NanDetectMode STALLED (21 CI failures, mostly unrelated, maintainers unresponsive)

### 2. GRPO NaN Detective Utility
- `tools/grpo_nan_detective.py` — 100-line NanDetectMode port for RTX 4090 GRPO
- Forward-pass NaN/Inf detection at ATen op granularity
- 500,000× faster than `torch.autograd.detect_anomaly()`
- Includes: shape/dtype/stats reporting, first-NaN-op capture, Inf detection, skip_ops filtering
- Based on PyTorch PR #187653 by rajfirke (albanD/subclass_zoo prototype)

### 3. P9-2 Per-Unit LoRA Analysis
- `tools/trl_p9_2_per_unit_lora_fsdp_analysis.py` — Design analysis document
- verl's `layered_summon_lora_params()` iterates individual FSDP units, not full model
- Peak memory: largest single FSDP unit → 10× reduction (60→6 GiB)
- TRL port challenge: Accelerate FSDPPlugin integration (not verl's custom FSDP)

### GPU Status
- University: Connection timed out — still OFFLINE
- Matpool: Connection refused — still OFFLINE

## Notes Created
- `tools/grpo_nan_detective.py` — NaN detective utility
- `tools/trl_p9_2_per_unit_lora_fsdp_analysis.py` — P9-2 design analysis

## Total Project Stats
- 444+ tools, 336+ project notes, 61 fundamentals notes, 1 synthesis
- Theory domains: 17

---
(Session 3 also on 2026-07-12 — see above)

# 2026-07-12 Diary (Session 3)

## Summary
Continued TRL contribution strategy with 3 new patches, identified TRL #6361 Qwen3.5 chat template bug root cause, analyzed Megatron #4590 RL hook architecture. GPUs still offline.

## Work Done

### 1. TRL Contribution Patches (3 new)
- `tools/trl_p8_1_grpo_group_size_warning.py` — P8-1: Warn when num_generations=1 (GRPO degenerates to REINFORCE, advantage=0). ~10 LOC, warning-only.
- `tools/trl_p7_2_top_n_sigma_reward_clipping.py` — P7-2: Optional advantage clipping to [mean-n*std, mean+n*std]. ~30 LOC, default=0 (disabled), recommended 3.0 for noisy rewards.
- Total TRL patches ready: 5 (P9-1, P8-1, P7-2, #6274 fix, #6361 fix)

### 2. TRL #6361: Qwen3.5 Chat Template Bug
- `tools/trl_6361_fix_qwen3_5_chat_template_think.py`
- **Root cause**: Qwen3.5 generation prompt ALWAYS emits `<think>\n` (unlike Qwen3). The training template's thinking detection checks `if '<think>' in content and '</think>' in content`, but when model response is stored without the prompt prefix, `</think>` is present but `<think>` is not → condition fails → empty `<think>\n\n</think>\n\n` block produced.
- **Fix**: Add `{%- elif '</think>' in content %}` branch to extract reasoning from the `</think>` closer.
- Affects both `qwen3_5_think_training.jinja` and `qwen3_5_nothink_training.jinja`.
- Issue opened 2026-07-11, 0 comments (untriaged).

### 3. Cross-Framework Scan
- **Megatron #4590** (14 comments): `output_processor` hook for RL extension point. Key finding: `calculate_per_token_loss=True` vs `False` produces **158% gradient bias** with imbalanced token counts (common in RL). This is the Megatron-Bridge #4515 RFC focus. chengcuiping validated the hook is bit-identical to monkey-patched path on 4×A100 with Qwen2.5-0.5B GRPO. veRL PR #6933 is the migration.
- **TRL open issues**: #6166 (NaN token from vLLM), #6361 (Qwen3.5 template), #6274 (Qwen VLM tool slice), #6354 (harbor/openreward)
- **Megatron open**: #5703 (DSA fused backends), #5683 (Dynamic-CP multimodal), #4590 (RL patching)
- **verl**: Search partially blocked
- **vLLM**: #48371 (MoE OOB expert_map fix), #47761 (Qwen3.6-35B-A3B-FP8)

### 4. Knowledge Map Updated
- Should add `megatron_rl_hook` domain for the output_processor extension point and gradient normalization bug

### GPU Status
- University: Connection timed out — still OFFLINE
- Matpool: Connection refused — still OFFLINE
- GitHub: Reachable (used gh CLI successfully)

## Notes Created
- `tools/trl_p8_1_grpo_group_size_warning.py` — group_size=1 detection warning
- `tools/trl_p7_2_top_n_sigma_reward_clipping.py` — reward clipping
- `tools/trl_6361_fix_qwen3_5_chat_template_think.py` — Qwen3.5 template fix

## Total Project Stats
- 441+ tools, 336+ project notes, 61 fundamentals notes, 1 synthesis
- Theory domains: 17 (transformer, grpo, quantization, optimizer, spec_decode, architecture, generative, rlhf, inductor, lifecycle, fsdp_spmd, ppo_vs_grpo, reward_shaping, trl_grpo, zero_optimization, cross_framework_grpo, megatron_rl_hook)
